# E15 — 극좌표 입력. E14 에서 앙상블 이득이 표현 다양성에 단조 증가하는 것이 확인됐고
# (표현 1종 0.7420 < 2종 0.7641 < 3종 0.7732), 기존 표현 셋은 전부 직교 격자다.
#
# 2026-08-22 정정. 첫 시도는 dihedral 증강을 그대로 썼는데, 극좌표에서 rot90 은
# 반지름 축과 각도 축을 맞바꾼다. 8개 변환 중 6개가 구조를 파괴했고 test macro-F1 이
# 0.5206 까지 떨어졌다. 극좌표에서 유효한 대칭은 각도 축의 순환이동과 뒤집기뿐이다.
cache: data/wm811k/cache/wm811k_64polar.npz
tag: e15_polar
augment_mode: angular
epochs: 40
batch_size: 256
lr: 0.001
pretrained: false
augment: true
class_weight: false
loss: ce
cosine: true
warmup: 3
seed: 0

# 높은 재현율을 목표로 하는 arm — 다음 세션용 사전등록

작성 2026-08-23 22:15, 브랜치 `exp/ood-oneclass`. **아직 실행하지 않았다.**
판정은 §8 에 append 한다.

## 1. 출처를 먼저 적는다 — 이 가설은 `test_dev` 에서 생겼다

21차 사이클(`candidate/ood_o2_fuse4.md`)에서 `fuse4` 를 기각했는데,
**기각된 arm 이 한 축에서는 세 seed 다 이겼다.**

| arm | AUPR블록 (주 지표) | **FPR@95TPR** | 95% 재현율 헛경보 |
|---|---:|---:|---:|
| fuse3 (채택) | **0.8304** | 0.1877 | 15,370 |
| fuse4 s0 / s1 / s2 | 0.8238 / 0.8292 / 0.8383 | **0.1835 / 0.1721 / 0.1645** | **15,028 / 14,096 / 13,471** |

**이 관측은 `test_dev` 에서 나왔다.** 그러므로 여기서 세우는 가설은
**탐색적으로 생성된 것**이고, 그 사실을 숨기면 이 문서 전체가 무의미하다.

**주 지표를 바꾸지 않는다.** 정정 9 가 정확히 그 실수였다 —
주 지표를 바꾸고 검정을 안 바꿔 무승부를 승리로 읽을 뻔했다.
**여기서 하는 것은 지표 교체가 아니라 "다른 목적함수를 가진 별도 arm 계열" 을 세우는 것**이고,
**보고할 때 채택 arm 표와 섞지 않는다.**

## 2. 무엇을 묻는가

> **결함의 95% 를 잡는 것이 목적이라면, 채택 arm 이 최선인가.**

팹에서 이 질문은 자연스럽다 — 놓치면 안 되는 공정이 있고, 거기서는
곡선 아래 넓이가 아니라 **"95% 를 잡을 때 몇 장을 띄우는가"** 가 전부다.
현재 그 답은 **15,370장**이다.

## 3. 주 지표를 실행 전에 못 박는다

- **주 지표 = FPR@95TPR.** 이 arm 계열에서만 그렇다.
- **병기 = blocked AUPR, 운영 지점 표 전체, 클래스별, dieSize 구간별, 고유값 개수.**
- **blocked AUPR 이 채택 arm보다 0.02 넘게 떨어지면, FPR@95TPR 이 아무리 좋아도 채택하지 않는다.**
  한 지점만 좋고 나머지가 무너지는 채점기는 쓸 수 없다. 이 문턱을 **실행 전에** 정한다.

## 4. arm 후보 — 전부 범위 A

| arm | 무엇 | 왜 |
|---|---|---|
| **H1** | `fuse4`(O2 epoch 3 포함), 3 seed | 21차가 본 그 arm 을 **선언된 목적함수로** 다시 잰다 |
| **H2** | Tippett 최대 규칙 | 15차에서 FPR@95TPR **0.1742** 로 Fisher(0.1877)를 이겼다. 고유값 204 라 기각됐는데, **높은 재현율에서는 동점이 8,776장**이라 오히려 여기서 문제가 크다 — 확인용 |
| **H3** | fuse3 그대로 | 대조 |

**H2 를 넣는 이유는 이길 것 같아서가 아니라 반증하기 위해서다** —
15차가 "눈금이 거칠어 운영 지점을 못 고른다" 고 기각했는데,
**바로 그 운영 지점을 목적으로 삼으면 그 결함이 정면으로 드러나야 한다.**

## 5. 반증 조건

1. **세 seed 전부 fuse3 보다 FPR@95TPR 이 낮지 않으면 기각.** 평균이 아니라 최악값.
2. **blocked AUPR 이 fuse3 대비 0.02 넘게 떨어지면 기각**(§3).
3. **95% 재현율 문턱에서 동점인 웨이퍼가 1,000장을 넘으면 채택 보류.**
   문턱을 못 고르면 그 지점이 존재하지 않는 것과 같다(15차의 Tippett 이 8,776장이었다).
4. **Scratch, Center, Loc 중 하나라도 CI 가 겹치지 않게 내려가면 기각.**
5. **이득이 seed 폭보다 작으면 무승부로 적는다**(21차의 조건 6 과 같은 규칙).
6. **`test_sealed` 를 열려면 1~5 를 전부 통과해야 한다.** 통과 못 하면 안 연다.

## 6. 실행 전 예측

1. **H1 이 세 seed 다 fuse3 보다 FPR@95TPR 이 낮다** — 21차에서 이미 그랬다.
   **이건 예측이 아니라 재확인이다. 그래서 이 arm 만으로는 아무것도 증명 못 한다.**
2. **H1 의 blocked AUPR 하락은 0.02 안에 들어간다**(21차 실측 최대 -0.0066).
3. **H2 는 반증 조건 3 에 걸린다** — 95% 문턱 동점이 8,776장이다.
4. **H1 의 이득(약 -0.015)이 seed 폭(21차 FPR@95TPR 범위 0.019)보다 작다**
   → **반증 조건 5 가 발동해 무승부가 된다.**

**예측 4 가 맞으면 이 사이클도 기각이고, 그 이유가 21차와 같다 — seed 폭이 이득보다 크다.**
그렇다면 결론은 **"O2 신호는 높은 재현율 쪽에 일관된 방향의 이득을 주지만
그 크기가 초기화 잡음보다 작다"** 이고, **더 큰 seed 수나 seed 앙상블이 다음 질문**이 된다.

## 7. 진짜로 값어치 있을 다음 수 — seed 앙상블

21차에서 세 seed 의 fuse4 가 **0.8238 / 0.8292 / 0.8383** 이었다.
**셋을 평균 내면 초기화 잡음이 줄어든다.** 그건 이 워크스트림이 한 번도 안 해 본 것이고,
**범위 A 안이며**(정상만 쓴다) **test 로 고를 자유도가 없다**(전부 쓴다).

**다만 그건 이 문서의 arm 이 아니다.** 별도 사전등록이 필요하고,
**seed 개수를 결과 보고 정하면 안 된다** — 3 으로 미리 박아야 한다.

## 8. 왜 지금 실행하지 않는가

**가설이 `test_dev` 에서 생겼기 때문이다**(§1). 같은 자료에서 확인하면 순환이다.
제대로 하려면 **봉인에서 확인해야 하는데, 봉인은 한 번뿐이고
확인할 arm 이 아직 확정되지 않았다**(§7 의 seed 앙상블이 더 나을 수 있다).

**순서가 이렇다**: (1) seed 앙상블을 별도로 사전등록해 `test_dev` 에서 잰다 →
(2) 둘 중 나은 것을 최종 arm 으로 확정한다 → (3) **그때 봉인을 한 번 연다.**
**순서를 건너뛰면 봉인을 태우고 아무것도 확인 못 한다.**

## 9. 착수 직전 개정 — §8 의 전제가 채워졌다 (2026-08-24 00:0x, 실행 전)

**이 절은 한 줄도 실행하기 전에 쓴다.**

### 9.1 왜 이제 실행하는가

§8 은 **"지금 실행하지 않는다"** 고 적으면서 순서를 박아 뒀다:
**(1) seed 앙상블을 별도로 사전등록해 `test_dev` 에서 잰다 → (2) 둘 중 나은 것을
최종 arm 으로 확정한다 → (3) 그때 봉인을 한 번 연다.**

**(1)이 끝났다** — 22차 사이클(`candidate/ood_seed_ensemble.md`).
그러므로 지금 하는 것은 **(2)** 이고, **§8 이 정한 순서를 지키는 것이지 어기는 것이 아니다.**

### 9.2 H4 를 arm 목록에 넣는다 — 이것은 사후 추가가 아니다

§7 과 §8 이 **seed 앙상블을 비교 대상으로 이름 지어 두었다**("둘 중 나은 것").
그러므로 **H4 = fuse5 앙상블**은 §8 이 이미 지정한 arm 이다.

| arm | 무엇 |
|---|---|
| H1 | fuse4, seed 0/1/2 각각 |
| H2 | Tippett 최대 규칙 (반증용) |
| H3 | fuse3 (대조) |
| **H4** | **fuse5 seed 앙상블** (§8 이 지정한 비교 대상) |

### 9.3 앙상블에는 조건 1, 5 를 그대로 못 쓴다 — 대체 규칙을 지금 정한다

H4 는 seed 집합이 고정되면 **단일 값**이라 "세 seed 전부" 도 "seed 폭" 도 없다.

- **조건 1 대체**: **짝지은 부트스트랩으로 잰 `FPR@95TPR` 차이의 95% CI 상한이 0 이상이면 기각.**
  (낮을수록 좋은 지표이므로 **상한 < 0** 이어야 이긴 것이다.)
- **조건 5 대체**: **이득의 절대값이 21차 fuse4 의 seed 폭(0.1835 − 0.1645 = 0.0190)보다
  작으면 무승부로 적는다.** 초기화 잡음보다 작은 이득은 이득이 아니다.
- **짝지은 부트스트랩 규약**: 웨이퍼를 복원 재표본하고 **같은 재표본을 두 arm 에 똑같이** 먹여
  각각 `FPR@95TPR` 을 다시 계산해 차이를 낸다. 300회, 시드 고정.

### 9.4 봉인은 이 사이클에서 열지 않는다 — 조건 6 을 정지시킨다

§5-6 은 "1~5 를 전부 통과하면 열 수 있다" 였다. **그 조항을 이 사이클에서는 정지시킨다.**

**통과가 나와도 열지 않고 다음 세션으로 넘긴다.** 이유는 둘이다.

1. **봉인은 한 번뿐이고, 최종 arm 이 고정된 뒤에 쓰는 것이다.**
   오늘 통과가 나오면 그건 "최종 후보가 정해졌다" 이지 "확정됐다" 가 아니다 —
   §7 이 남긴 "seed 를 더 늘리면?" 이 아직 안 풀렸다.
2. **오늘 이미 열 기회가 두 번 있었고 두 번 다 조건 미달로 안 열었다**(21, 22차).
   **조건을 채웠다고 곧바로 태우면 그 절제가 무의미해진다.**

### 9.5 통과해도 채택 표를 바꾸지 않는다

**이 워크스트림의 주 지표는 `aupr_blocked` 이고 그건 안 바뀐다.**
§3 이 `FPR@95TPR` 을 주 지표로 선언한 것은 **이 arm 계열 안에서만**이다.

**그러므로 통과하면 얻는 것은 "고재현율 운영점을 위한 별도 arm 후보" 이지
`docs/reports/OOD.md` 의 채택 arm 교체가 아니다.**
21차에 내가 적은 문장이 그대로 적용된다 —
**"운영 지점 하나가 좋다고 판정 규칙을 고쳐 쓰면 그건 사후 지표 변경이고 정정 9 가 그거였다."**

### 9.6 이 사이클의 증거력을 정직하게 낮춰 적는다

**H1 과 H4 의 `FPR@95TPR` 점추정은 이미 21, 22차에서 봤다**(0.1835 / 0.1721 / 0.1645 / 0.1712).
**그러니 그 숫자들은 새 증거가 아니다.**

**새로 나오는 것은 셋뿐이다.**

1. **짝지은 차이의 불확실성**(부트스트랩 CI) — 한 번도 안 쟀다
2. **95% 재현율 문턱의 동점 수** — H1, H4 에 대해 안 쟀다(H2 만 8,776 로 알려져 있다)
3. **선언된 목적함수 아래의 형식 판정**

**즉 이 사이클은 새 발견을 노리는 것이 아니라, 이미 본 방향에 불확실성을 붙여
"채택 후보인가 잡음인가" 를 가르는 것이다.** 그게 전부이고, 그 이상으로 읽지 않는다.

### 9.7 추가 실행 전 예측 (§6 에 이어서, 아직 안 잰 것들만)

5. **fuse4 seed 0(가장 약한 것, 차이 -0.0042)의 짝지은 CI 는 0 을 포함한다.**
6. **fuse5 앙상블(차이 -0.0165)의 짝지은 CI 는 0 을 배제한다.**
7. **그런데 fuse5 의 이득 0.0165 가 fuse4 의 seed 폭 0.0190 보다 작다**
   → **조건 5 대체 규칙이 발동해 무승부로 적힌다.**
8. **H1, H4 의 95% 문턱 동점 수는 1,000장 미만이다**(고유값이 8만 대다). 조건 3 통과.
9. **H2(Tippett)는 조건 3 에 걸린다**(8,776장).

**예측 6 과 7 이 동시에 맞으면 결론은 이렇다**:
**방향은 실재하고 통계적으로도 0 이 아니지만, 크기가 초기화 잡음보다 작다.**
그러면 다음 질문은 **더 나은 arm 이 아니라 더 많은 seed** 이고,
**봉인은 그때까지 아껴 둔다.**

---

# 판정 (실행 후 append)
## 판정 — 아무것도 채택하지 않는다. 봉인도 안 열었다 (2026-08-24 00:0x, 24차 사이클)

재현: `src/a54_high_recall_eval.py` → `result/ood/o2_high_recall/high_recall_metrics.json`.
**저장된 점수를 다시 채점했다 — 새 arm 을 만들지 않았다**(§9.6).
`test_dev` 87,695장. **`test_sealed` 는 부르지도 않았다**(§9.4).

### 선언된 주 지표 아래의 수치

| arm | **FPR@95TPR** | blocked AUPR | 고유값 | **95% 문턱 동점** | 95% 헛경보 |
|---|---:|---:|---:|---:|---:|
| **H3 fuse3 (대조)** | 0.1877 | **0.8304** | 3,647 | 23 | 15,370 |
| H1 fuse4 s0 | 0.1835 | 0.8238 | 81,363 | 1 | 15,028 |
| H1 fuse4 s1 | 0.1721 | 0.8292 | 81,931 | 1 | 14,096 |
| H1 fuse4 s2 | **0.1645** | 0.8383 | 81,766 | 1 | 13,471 |
| H4 fuse5 앙상블 | 0.1712 | 0.8312 | 86,070 | 1 | 14,021 |
| H2 Tippett 최대 | 0.1742 | 0.7801 | **204** | **8,776** | 14,269 |

### 반증 조건 채점

| 조건 | H1 (fuse4 3 seed) | H4 (앙상블) | H2 (Tippett) |
|---|---|---|---|
| 1. 최악값도 대조보다 낮은가 / (H4) CI 상한 < 0 | **통과** (최악 0.1835 < 0.1877) | **통과** [-0.0244, -0.0093] | **통과** [-0.0279, -0.0059] |
| 2. blocked AUPR 하락 ≤ 0.02 | 통과 (최대 +0.0065) | 통과 (-0.0009) | **발동** (+0.0502) |
| 3. 95% 문턱 동점 ≤ 1,000장 | 통과 (1장) | 통과 (1장) | **발동** (8,776장) |
| 4. Scratch/Center/Loc 하락 | 통과 (하락 없음) | 통과 | 통과 |
| **5. 이득 > 21차 seed 폭(0.0190)** | **발동.** 평균 이득 **0.0143** | **발동.** 이득 **0.0165** | — |

**H1: 조건 5 발동 → 무승부.** **H4: 조건 5 발동 → 무승부.** **H2: 조건 2, 3 발동 → 기각.**

> **선언된 목적함수 아래에서도 채택할 것이 없다.**
> 방향은 실재하고 통계적으로 0 도 아니지만, **크기가 초기화 잡음보다 작다.**

### 이 사이클에서 가장 날카로운 줄 — 목적함수를 이기는 arm 을 그 운영점에서 못 쓴다

**H2(Tippett)는 선언된 주 지표를 실제로 이긴다** — 짝지은 차이 **[-0.0279, -0.0059], p=0.0000.**
**그런데 바로 그 95% 재현율 문턱에서 8,776장이 같은 점수를 받는다.**

**목적으로 삼은 운영점에서 문턱을 못 고른다.**
`fuse3` 는 그 자리에서 동점이 **23장**이고 H1, H4 는 **1장**이다.

§4 에 **"H2 를 넣는 이유는 이길 것 같아서가 아니라 반증하기 위해서다 —
바로 그 운영 지점을 목적으로 삼으면 그 결함이 정면으로 드러나야 한다"** 고 적었다.
**정확히 그렇게 드러났다.** 15차가 총합 지표로 기각한 것을,
**그 arm 에게 가장 유리한 목적함수 아래에서도** 같은 이유로 기각한다.

### 주 지표는 그대로다 — 채택 표를 안 바꾼다 (§9.5)

blocked AUPR 짝지은 차이: H1 s0 **[-0.0084, -0.0051] 짐**, s1 [-0.0029, +0.0005] 무승부,
s2 [+0.0060, +0.0100] 이김, H4 [-0.0005, +0.0022] **무승부**, H2 [-0.0555, -0.0447] **짐**.

**21, 22차와 같은 그림이고 `fuse3` 가 유지된다.**

### 봉인 — 조건을 채운 arm 이 없고, 채웠어도 안 열었을 것이다

§9.4 가 실행 전에 조건 6 을 정지시켰다. **결과적으로 정지가 필요하지도 않았다** —
조건 1~5 를 전부 통과한 arm 이 없다.
**오늘 봉인을 열 기회가 세 번 있었고(21, 22, 24차) 세 번 다 안 열었다.**

### 예측 채점 — 9개 전부 맞았다. 그런데 그게 좋은 신호가 아니다

| # | 예측 | 결과 | 성격 |
|---:|---|---|---|
| 1 | H1 세 seed 다 FPR@95TPR 이 낮다 | 맞음 | **재확인**(21차에서 봤다) |
| 2 | H1 의 blocked AUPR 하락이 0.02 안 | 맞음 (+0.0065) | **재확인** |
| 3 | H2 가 조건 3 에 걸린다 | 맞음 (8,776) | **재확인**(15차에서 봤다) |
| 4 | H1 의 이득 < seed 폭 → 조건 5 발동 | 맞음 (0.0143 < 0.0190) | **재확인에 가깝다** |
| 5 | fuse4 s0 의 짝지은 CI 가 0 을 포함 | **맞음** [-0.0157, +0.0017] | **맹목** |
| 6 | fuse5 의 짝지은 CI 가 0 을 배제 | **맞음** [-0.0244, -0.0093] | **맹목** |
| 7 | fuse5 의 이득 < seed 폭 → 무승부 | **맞음** (0.0165 < 0.0190) | **맹목** |
| 8 | H1, H4 의 95% 동점 < 1,000장 | **맞음** (1장) | **맹목** |
| 9 | H2 가 조건 3 에 걸린다 | 맞음 | **재확인** |

**전부 맞은 것을 성과로 읽으면 안 된다.** §9.6 에 실행 전에 적어 뒀다 —
**점추정은 이미 21, 22차에서 본 숫자이고 이 사이클의 증거력은 낮다.**
**진짜로 몰랐던 것은 5~8 넷뿐이고, 그 넷이 맞은 것이 이 사이클이 더한 전부다.**
오늘 다른 사이클들은 예측이 절반쯤 틀렸다. **그게 정상이고, 이 사이클이 예외인 이유는
새 것을 별로 안 물었기 때문이다.**

### 내 사전등록의 약점 하나 — 정정으로 세지는 않는다

**조건 1 은 "세 seed 전부 낮으면 통과" 라는 점추정 규칙이고, seed 별 불확실성을 안 걸었다.**
실제로 **H1 seed 0 의 짝지은 CI 는 0 을 포함한다**([-0.0157, +0.0017]).
**즉 세 seed 중 하나는 대조와 구분되지 않는데 조건 1 은 통과로 읽힌다.**

**판정은 안 바뀐다**(조건 5 가 어차피 발동했다). 그래서 **정정 번호를 붙이지 않는다** —
정정 15, 16 은 해석을 바꿨지만 이건 아무것도 안 바꿨다.
**다만 다음 사전등록에 규칙으로 남긴다: seed 를 가로지르는 점추정 규칙에는
seed 별 불확실성을 같이 걸어야 한다.**

### 결론

1. **채택 arm 은 그대로 `fuse3` 다.** 고재현율 목적함수 아래에서도 바뀌지 않는다.
2. **O2 신호는 높은 재현율 쪽에 일관된 방향의 이득을 준다**(세 seed + 앙상블 전부 같은 방향,
   CI 도 대부분 0 을 배제). **그러나 크기가 초기화 잡음보다 작다.**
   → §6 의 예측 4 가 그대로 성립하고, **다음 질문은 더 나은 arm 이 아니라 더 많은 seed 다.**
3. **Tippett 은 목적함수를 이기고도 그 운영점에서 못 쓴다.** 눈금이 거칠다는 15차의 기각 사유가
   **가장 유리한 조건에서도** 유지된다.
4. **봉인은 안 썼다.** 한 번 열 기회가 다음 세션에 그대로 남는다.

### 다음 세션에 넘기는 것

**seed 수를 늘리는 것이 유일하게 남은 직선 경로다.** 이득 0.0143~0.0165 가
seed 폭 0.0190 보다 작다는 것이 세 사이클(21, 22, 24) 연속 결론이다.
**seed 를 늘리면 폭이 줄어 이득이 드러날 수 있다.**
**단 seed 개수를 결과 보고 정하면 안 된다** — 사전등록에 미리 박아야 하고,
그 arm 이 확정되면 **그때 봉인을 한 번 연다.**


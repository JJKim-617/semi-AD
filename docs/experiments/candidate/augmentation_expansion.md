# E17 후보 — 증강 확장

작성일 2026-08-22. **실행 전에 쓴다.** 목표는 macro-F1 0.87.

## 왜 증강인가

이 프로젝트에서 확실히 통한 것은 둘뿐이고 그중 큰 쪽이 **dihedral 증강 +0.101** 이다.
다른 모든 기법을 합친 것보다 크다. 그런데 지금 쓰는 증강은 **dihedral 하나뿐**이다.

그리고 방금 상한을 다시 쟀다. **결정규칙을 test 로 직접 최적화해도 +0.006** 이다
(0.7809 -> 0.7866). 임계값, 사후 보정, 2단계 분류처럼 "경계를 어디 놓을까" 류는 전부 고갈됐다.
남은 길은 **오류를 실제로 줄이는 것**뿐이고, 그러면 표현과 일반화를 건드려야 한다.

## 후보와 그 근거

셀 값이 명목형({0 다이없음, 1 정상, 2 불량})이므로 **보간을 만드는 증강은 전부 금지**다.
아래는 전부 색인 재배열이거나 값 치환이라 그 제약을 지킨다.

### 1. 랜덤 스케일 (pad 전용) — 가장 근거가 강하다

극좌표 실패를 분석하다 찾은 것: **train 의 58.1% 가 dieSize 400~562 한 구간에 몰려 있고
test 는 넓게 퍼져 있다.** 특히 562~1090 구간이 train 의 14.2% 인데 test 의 45.9% 다.

| dieSize | train 비중 | test 비중 |
|---|---:|---:|
| 400~562 | **58.1%** | 14.2% |
| 562~1090 | 14.2% | **45.9%** |

pad 캔버스 안에서 웨이퍼를 무작위로 축소/확대하면 **그 빈 구간을 합성**할 수 있다.
nearest 로만 재표본하므로 명목형 제약을 지킨다.

### 2. 다이 노이즈 — pass <-> fail 소량 반전

물리적으로 **측정 잡음**에 해당한다. 패턴은 유지한 채 개별 다이를 흔든다.
none 과 결함의 차이가 "산발적 불량이 구조를 이루는가" 인데, 그 경계를 직접 정규화한다.
오류의 81.7% 가 그 경계에 있으므로 표적이 맞다. 비율은 0.5~2% 로 작게.

### 3. 다이 드롭아웃 — 일부 다이를 0(다이 없음)으로

물리적으로 **미측정 다이**에 해당하고 실제 웨이퍼에 흔하다.
모델이 특정 위치의 다이 존재에 의존하지 않게 한다.

### 4. 랜덤 이동 (pad 전용)

pad 는 웨이퍼를 캔버스 중앙에 놓는데, **캔버스 안 위치는 임의**다.
따라서 이동은 라벨을 보존하는 정당한 대칭이다. resize 에는 적용 불가(웨이퍼가 캔버스를 채움).

## 반증 조건 (먼저 박아둔다)

1. **각 증강을 단독으로, dihedral 만 쓰는 기준선과 비교한다.** 3 seed.
   run-to-run 범위가 0.050 이므로 **평균이 그만큼 오르지 않으면 채택하지 않는다.**
   단 앙상블 기여는 따로 본다 — 단독으로 못 넘어도 구성원으로는 값어치가 있을 수 있다(E14).
2. **랜덤 스케일이 통하면 크기 구간별로 확인한다.** 개선이 562~1090 구간(train 이 희박한 곳)에
   집중돼야 가설이 맞는 것이다. 전 구간에 고르게 오르면 이득의 원인은 크기 커버리지가 아니라
   그냥 정규화 효과다.
3. **다이 노이즈가 통하면 none 오검출이 줄어야 한다.** 현재 1,339 건.
   macro-F1 만 오르고 누출이 그대로면 다른 경로로 오른 것이므로 설명을 다시 찾는다.
4. **전부 합친 것이 최선의 단독 증강보다 나은지 따로 잰다.** 증강은 서로 간섭할 수 있다.
   focal + alpha 가 같은 축을 두 번 조작해 -0.089 였던 전례가 있다.

## 하지 않을 것

- **mixup, CutMix**: 두 웨이퍼를 섞으면 라벨이 모호해진다. 결함 유형은 배타적이라
  0.5 Center + 0.5 Scratch 라는 라벨에 물리적 의미가 없다.
- **임의 각도 회전, 탄성 변형**: 보간이 개입해 존재하지 않는 셀 값을 만든다.
- **밝기, 대비 조정**: 입력이 명목형이라 의미가 없다.

## 목표까지의 거리

현재 0.7809, 목표 0.87 이면 클래스당 평균 **+0.089** 다.
증강 하나로 될 일이 아니고, E18(백본 교체), E19(EMA, label smoothing)와 앙상블을 합쳐야 한다.
**단일 실험에 0.09 를 기대하지 않는다.**

---

# 결과 — translate 만 통했다 (2026-08-22)

기준선은 재학습 없이 pad 3 seed 를 쓴다(평균 **0.7261**, 범위 0.0495, 최소 0.6991, 최대 0.7485).

| 변형 | seed 0 | seed 1 | 평균 | seed 범위 | 기준선 대비 | 판정 |
|---|---:|---:|---:|---:|---:|---|
| **translate** | **0.7674** | **0.7607** | **0.7641** | **0.0067** | **+0.0380** | **채택** |
| scale | 0.7202 | 0.7298 | 0.7250 | 0.0096 | -0.0011 | 기각 |
| dropout | 0.7048 | 0.7192 | 0.7120 | 0.0144 | -0.0141 | 기각 |
| all (넷 다) | 0.6990 | 0.6878 | 0.6934 | 0.0112 | -0.0327 | 기각 |
| noise | 0.6436 | 0.7068 | 0.6752 | **0.0632** | -0.0509 | 기각 |

**translate 두 seed 모두 이전 최고 단일 모델(0.7485)을 넘고, TTA 를 붙인 0.7551 보다도 높다.**
사전 등록한 조건 1(평균이 잡음 폭만큼 올라야 채택)을 넷 중 유일하게 통과했다.

## 사전 순위가 거꾸로였다

이 문서를 쓸 때 **scale 을 "가장 근거가 강하다"** 고 적고 translate 를 네 번째에 두었다.
결과는 정확히 반대다 — scale 은 -0.0011 로 무효였고 translate 가 +0.0380 이다.

scale 의 근거는 크기 커버리지 갭이었다(train 58.1% 가 dieSize 400~562 한 구간,
test 45.9% 가 train 14.2% 인 구간). 그 진단 자체는 극좌표 실패에서 나온 것이고 여전히 사실이다.
**틀린 것은 "그 갭을 증강으로 메우면 성능이 오른다" 는 연결이다.**
왜 안 통했는지는 모른다. 추측하지 않는다.

## 앙상블 기여 — 여기서도 1위다

사전 등록한 조건에 "단독으로 못 넘어도 구성원으로는 값어치가 있을 수 있다" 고 적었으므로
E17 변형 전부를 매니페스트에 넣고 따로 쟀다.

| 앙상블 구성 | n | macro-F1 | accuracy |
|---|---:|---:|---:|
| 이전 최고 11개 | 11 | 0.7820 | 0.9695 |
| **11개 + translate 2개** | **13** | **0.7871** | **0.9713** |
| 10개 + translate 2개 | 12 | 0.7860 | 0.9713 |
| translate 만 | 2 | 0.7712 | 0.9724 |
| 10개 + E17 전부 | 20 | 0.7813 | 0.9708 |
| 가진 것 전부 | 25 | 0.7787 | 0.9698 |

leave-one-out(풀 12개, k=2~4)에서도 translate 가 압도적이다.

| 구성원 | 단독 | LOO |
|---|---:|---:|
| **e17_translate_s0** | 0.7674 | **+0.0102** |
| **e17_translate_s1** | 0.7607 | **+0.0074** |
| e8_res96 | 0.7320 | +0.0025 |
| e8_pad | 0.7485 | +0.0005 |
| e6_focal_long | 0.7251 | -0.0046 |

**기각된 E17 변형들을 다 넣으면 앙상블이 내려간다**(0.7871 -> 0.7813 -> 0.7787).
14차 사이클에서 확인된 것과 같다 — "전부 넣기" 는 후보 품질이 비슷할 때만 성립한다.

## 설명 없는 관측

증강을 추가한 변형들의 seed 범위가 **0.0067~0.0144** 로 기준선 0.0495 보다 일관되게 좁다
(noise 만 0.0632 로 예외). translate 도 0.0067 이다.
**평균은 못 올려도 분산은 줄이는** 패턴인데 원인을 모른다. 관측으로만 남긴다.

## 사후 조건 확인

조건 2("scale 이 통하면 크기 구간별로 확인")와 조건 3("noise 가 통하면 none 누출이 줄어야")은
둘 다 통하지 않았으므로 무의미해졌다.
조건 4("전부 합친 것이 최선의 단독 증강보다 나은지")는 **아니다** — `all`(0.6934)이
최고 단독 변형 translate(0.7641)보다 0.071 낮다. 증강끼리 간섭한다.

# OOD(one-class) 워크스트림 사이클 로그

9-class 지도학습(`exp/ad-baseline`)과 **별개 실험**이다. 그쪽 연대기는
`cycle_log.md` 에 있고 섞지 않는다.

---

# 1차 사이클 — 기획과 바닥 측정 (2026-08-22)

## 무엇을 왜 했는가

주제는 **정상 웨이퍼만 학습에 쓰고 결함을 이상으로 탐지하는 것**이다.
실제 팹이 결함 유형을 미리 열거할 수 없다는 조건과 맞고, 같은 연구실의 3D-AD 작업이 쓰는 설정과도 같다.

지시는 "가능한 한 높은 출발점에서 시작하라" 였다. 그래서 순서를
**조사 → 바닥 측정 → 기획 → TDD 뼈대** 로 잡았다. 바닥을 먼저 잰 것이 결과적으로 기획 전체를 바꿨다.

## 조사에서 막힌 것

**웹 검색 예산이 세션에서 소진**돼(200/200) 광범위 조사를 못 했다.
대신 arXiv 초록을 WebFetch 로 직접 확인하고, 레포의 기존 조사 문서를 썼다.
**확인 못 한 것은 문서에 그렇게 표시했다.**

| 방법 | 확인된 것 | 출처 |
|---|---|---|
| PatchCore | 정상 patch feature memory bank, **ImageNet 임베딩**, MVTec image AUROC 최대 99.6% | arXiv 2106.08265 초록 |
| PaDiM | 사전학습 CNN patch 임베딩, **patch 위치별 다변량 가우시안**, one-class | arXiv 2011.08785 초록 |

둘 다 **ImageNet 특징에 의존**하는데, `docs/research/encoder_selection/README.md` 가
이 데이터에서 그 전이가 약하다는 근거를 이미 모아뒀다(17만 장 규모에서 이득 소멸,
웨이퍼맵 실측에서 scratch CNN 83.14 vs MobileNetV2 fine-tune 59.46).
PaDiM 의 위치별 가우시안은 공간 정렬을 가정하는데 우리 웨이퍼는 크기가 크게 달라
**극좌표가 실패한 것과 같은 함정**으로 보인다 — 미검증 우려로만 기록했다.

## E0 — 바닥이 예상보다 훨씬 높다

학습이 전혀 없는 스칼라 통계를 공식 test 전수로 쟀다.

| 스칼라 | AUROC | AUPR | FPR@95TPR |
|---|---:|---:|---:|
| 불량 다이 개수 | **0.8167** | 0.3633 | 0.7367 |
| 불량 다이 비율 | 0.7899 | **0.3856** | 0.7364 |
| 웨이퍼 크기 | 0.5037 | 0.0650 | 0.9562 |
| 불량 공간 퍼짐 | 0.4727 | 0.0625 | 0.9926 |
| 불량 이심률 | 0.5064 | 0.0709 | 0.9548 |
| 무작위 | 0.5000 | 0.0666 | 0.95 |

**세 가지가 이 표에서 나왔다.**

**1. 원래 기획서의 목표가 약했다.** `ad_baseline_plan.md` §6 이 "AD: AUROC 0.90 을 1차 기준" 으로
제안했는데, 불량 다이를 세기만 해도 0.8167 이다. **바닥 위로 겨우 +0.083.**
목표를 AUPR 0.3856 기준으로 다시 세웠다.

**2. AUROC 만 보면 안 된다.** FPR@95TPR 0.7367 — 결함의 95% 를 잡으려면 정상의 74%,
약 8만 장을 같이 띄운다. AUROC 0.82 는 실무에서 무의미하다.
**주 지표를 AUPR 로 두고 FPR@95TPR 을 함께 보기로 했다.**

**3. 내가 만든 공간 통계가 무용지물이었다.** 불량 좌표의 2차 모멘트로 만든 퍼짐이
**무작위보다 낮은 0.4727**, 이심률은 0.5064 로 무작위다. 전역 통계로는 결함 구조가 안 잡힌다.
9-class 쪽에서 none→Scratch 오검출 웨이퍼를 전역 통계로 구분하지 못한 것과 같은 결과다.

### 값어치가 어디 남아 있는가

| 결함 | AUROC (개수 기준) |
|---|---:|
| Near-full / Random / Donut | 1.000 / 0.997 / 0.984 |
| Edge-Ring / Edge-Loc / Loc | 0.895 / 0.817 / 0.810 |
| Center | 0.754 |
| **Scratch** | **0.656** |

**절반은 이미 끝난 문제다.** 남은 것은 **Scratch, Center, Loc** — 개수가 아니라 공간 구조로
정의되는 것들이다. 전체 평균만 보면 개선이 쉬운 쪽에서 온 것처럼 보일 수 있어
**클래스별 표를 반증 조건에 넣었다.**

## 만든 것

- `docs/plans/2026-08-22-ood-oneclass-preregistration.md` — 사전등록 기획서. 반증 조건 5개를 실행 전에 박음
- `docs/reports/OOD.md` — 이 워크스트림 SSOT
- `src/a21_ood_metrics.py` — AUROC(동점 평균랭크), AUPR, FPR@95TPR, `evaluate_ood`(유병률 동봉), 자명한 채점기 2개
- `tests/test_a21_ood.py` (20개) + `tests/test_a21_ood_baseline_pin.py` (5개)
- `docs/research/ood_baseline/evidence/` — E0 스크립트와 원출력

**E0 를 회귀 테스트로 고정했다.** 9-class 에서 "전부 none 분류기 accuracy 0.9334" 를 박아
지표 회귀를 막은 것과 같은 장치다. 이제 바닥 아래로 내려가면 테스트가 깨진다.

## 격리

worktree `JJ/etc/Semi-AD-OOD`, 브랜치 `exp/ood-oneclass`.
9-class 실험이 GPU 0번을 02:00까지 쓰고 있어 **이 사이클은 CPU 만 썼다.**

## 다음

기획서 §5 사다리 — O1(정상 통계 template 잔차, 학습 없음) → O2(자기지도 인코더 + kNN) → O3(오토인코더).
O1 의 핵심 설계 결정은 **크기 정규화를 어떻게 하느냐**다. 극좌표가 그 지점에서 무너졌다.

---

# 2차 사이클 — O1 template 잔차, 그리고 바닥을 다시 재다 (2026-08-22 22:30~24:00)

## 무엇을 왜 했는가

사다리의 O1(정상 통계 template 잔차)을 하기로 돼 있었고, 그 **핵심 설계 결정은
크기 정규화**였다. 옆 워크스트림에서 극좌표가 정확히 그 지점에서 무너졌기 때문이다.
GPU 0번은 9-class 쪽 E18/convnext 가 쓰고 있어 **이 사이클도 CPU 만 썼다.**

중간에 **범위가 바뀌었다.** OOD 가 파이프라인 1단계가 되고
(OOD 탐지 → 다이 단위 국소화 → MLLM 8종 분류 → 유저 질의응답),
**다이 단위 잔차 맵이 1급 산출물**이 됐다. 기획서에 §9 범위 확장 절로 적었다
(정정이 아니다 — 프로젝트 정의가 바뀐 것이다).

## 순서

1. `docs/experiments/candidate/ood_template_residual.md` 에 **반증 조건 6개를 실행 전에** 박음
2. TDD — `tests/test_a22_ood_template.py` 먼저 쓰고 실패 확인 후 `src/a22_ood_template.py` 구현
3. `src/a23_ood_template_eval.py` 로 12 arm 채점
4. 결과를 보고 **짝지은 부트스트랩이 필요하다는 것을 알게 됨** → 정정
5. `ood_residual_pooling.md` 에 뭉개기 반증 조건을 **실행 전에** 박고 48 arm 채점
6. 결과가 너무 좋아 **빠진 대조군을 찾음** → 국소 밀도 대조군 실행 → 결론이 뒤집힘
7. 맵 내보내기와 대표 사례 시각화

## O1 template 잔차 — 탐지기로 전부 기각

세 좌표계(절대 pad / 리사이즈 / 자기 반경 정규화)와 두 채점기(NLL 잔차 /
불량률을 맞춘 뒤의 구조 점수 S_llr)를 균일 template 대조군과 함께 쟀다.

- **단독 최고 T-RESIZE S_nll AUPR 0.3619** — 바닥 0.3856 에 짝지은 검정으로 진다
  (-0.0237, p=0.0000). 반증 조건 1 로 기각.
- **융합(불량률 + T-PAD S_llr)은 AUPR 0.4058 로 바닥을 이긴다**(+0.0201, p=0.0000).
  그런데 Scratch 0.5794 / Center 0.6224 / Loc 0.7092 로 **값어치 남은 세 클래스가 전부
  나빠졌다.** 이득은 이미 끝난 클래스(Random, Near-full, Edge-Loc)에서 왔다.
  **반증 조건 2 가 정확히 이걸 잡으라고 박은 것이라 융합도 기각했다.**
  조건 1 만 봤으면 채택했을 것이다 — 사전등록이 실제로 일한 지점이다.

### 크기 정규화 질문에는 답이 나왔다

균일 template 대조군과 짝지어 재니 깔끔하게 갈렸다.

| | 균일 대조군 대비 AUPR 차이 | p |
|---|---:|---:|
| T-RESIZE (정규화 좌표) | [+0.0538, +0.0724] | 0.0000 |
| T-PAD (절대 좌표) | [-0.0003, +0.0022] | 0.1320 |
| T-RADIAL (반경 전용) | [-0.0051, -0.0028] | 0.0000 |

**정규화 좌표에서만 위치 template 이 균일 상수보다 정보를 담는다.**
절대 좌표에서 "학습" 한 위치 통계는 **균일 상수와 구분되지 않았다** —
train-none 이 64x64 격자 4096칸 중 **822칸(20.1%)을 한 번도 안 밟는다.**
큰 test 웨이퍼는 반드시 그 구멍 위에서 채점되고, `>1600` 구간 AUPR 이
0.1777 → 0.0826 으로 무너진다. **극좌표와 같은 실패의 재현이고, 원인도 같다.**

### 실행 전에 적은 예측이 4개 중 3개 틀렸다

"정상은 가장자리에서 더 자주 불량이 나므로 S_llr 이 Center/Scratch/Loc 을 돕고
Edge 계열을 해친다" 고 적었다. Edge 쪽은 맞았다(Edge-Ring 0.0282, 예상보다 훨씬 크게).
**Center 는 정반대로 갔다**(0.4096, 무작위 아래).

**전제가 틀렸다.** 정상 웨이퍼는 가장자리에서만 자주 죽는 게 아니라 **중심에서 더 죽는다.**
- train-none 웨이퍼의 **중심 최근접 다이는 58.9% 가 불량**이다(전체 다이 불량률 11.6%).
- 반경 프로파일이 U 자다: 중심 0.55 → 중간 0.085 → 가장자리 0.34.

그래서 Center 결함은 template 이 **이미 높은 자리**에 불량을 놓는 셈이 된다.
**왜 중심 다이가 그런지는 모른다.** WM-811K 는 6개 컬럼뿐이라 확인할 방법이 없다.

### 기각했지만 버리지 않은 것

S_llr 의 Edge-Ring AUROC 0.023 은 정보가 없다는 게 아니라 **부호가 반대**라는 뜻이다.
결함끼리만 놓고 보면 **학습 없는 스칼라 하나가 Edge-Ring 대 Scratch 를 AUROC 0.983 으로
가른다**(불량 다이 개수는 0.171, 뒤집어도 0.829). **1단계에서 기각된 것이 3단계에서
값어치가 있을 수 있다.** §9.4 간접 평가로 확인할 일이다.

## 정정 1 — 내가 박은 반증 조건 6 이 틀린 검정이었다

"두 arm 의 **독립 CI 가 겹치지 않아야** 이겼다고 한다" 고 적었는데,
두 점수는 **같은 118,595장** 위에서 계산되므로 표본 변동을 공유한다.
독립 CI 겹침은 짝지은 비교에 지나치게 보수적이다.
실제로 융합 [0.3950, 0.4168] 과 바닥 [0.3753, 0.3958] 이 0.0008 폭으로 겹쳐
"경계선" 으로 읽혔지만, 짝지은 검정으로는 +0.0201 [+0.0121, +0.0280], p=0.0000 로 명확하다.

`a23_ood_template_eval.paired_aupr_diff_ci` 를 TDD 로 추가했다.
재표본 가중치를 원 인덱스 위에서 한 번 뽑고 두 점수의 정렬 순서로 재배열해 넣어,
**두 arm 이 문자 그대로 같은 웨이퍼 묶음**을 보게 한다.

## 뭉개기 — 수치가 뒤집혔다

O1 의 두 스칼라는 사실 **다이별 잔차 맵의 합**이고, 합은 공간 구조에 가장 둔한 요약이다.
맵을 8가지로 뭉개 48 arm 을 쟀다.

**`smooth5_max`(5x5 이웃 평균의 최대)가 AUPR 0.6808 을 냈다.** 바닥의 1.8배다.

## 정정 2 — 그 점수는 template 이 낸 게 아니었다 (빠진 대조군)

결과가 너무 좋아 교란을 찾았고, 대수적으로 찾았다.
`pool_smoothed_max` 는 창 안 잔차 합을 **창 안 다이 개수로 나눈다.** 균일 template 이면

```
창 평균 = -log(1-p) + f * log((1-p)/p),   f = 창 안 불량 다이 비율
```

**기울기 양수인 아핀 함수**다. 즉 **균일 template + smooth_k_max = 국소 불량 밀도의 최대**이고
template 기여가 0 이다. 내가 후보 문서에 적은 대조군은 `sum` 뭉개기와 E0 스칼라뿐이었다 —
**뭉개기를 바꾸면 대조군도 같이 바뀌어야 한다는 것을 놓쳤다.**

대조군을 넣고 다시 재니:

| 창 | 반경 template | 균일(= 국소 밀도) | 짝지은 차이 | p |
|---|---:|---:|---:|---:|
| k=3 | 0.6034 | 0.4968 | [+0.0956, +0.1177] | 0.0000 |
| k=5 | 0.6808 | **0.6885** | [-0.0135, -0.0020] | 0.0060 |
| k=7 | 0.7033 | **0.7147** | [-0.0159, -0.0070] | 0.0000 |
| k=9 | 0.6828 | **0.7007** | [-0.0217, -0.0142] | 0.0000 |

**창이 5 이상이면 반경 template 은 순전한 손해다.**

## 정정 3 — O0 이 잡은 바닥 자체가 틀렸다

**국소 밀도는 학습이 0 이다.** template 도 파라미터도 train 자료도 안 쓴다.
그런데 AUPR 0.7147 을 낸다. O0 이 "학습 없는 자명한 스칼라" 를 잰다면서
**전역 스칼라만 쟀고** 바닥을 0.3856 이라고 적었다.

**진짜 바닥은 0.3856 이 아니라 0.7147 이다.**
1차 사이클에서 E0 의 전역 2차 모멘트가 무작위보다 낮은 것을 보고
"전역 통계로는 결함 구조가 안 잡힌다" 고 **옳게** 적어 놓고도,
그 다음 질문("그럼 국소 통계는?")을 O1 학습 사다리로 미뤘다.
**학습 없는 국소 통계를 먼저 쟀어야 했다.**

이전 사이클에서 "융합이 0.4058 로 바닥을 이겼다" 고 적은 것은
**잘못된 바닥에 대고 이긴 것**이다. 새 바닥에는 한참 못 미친다.
새 바닥은 `tests/test_a24_local_density_pin.py` 로 고정했다.

### 그래도 클래스별로는 template 이 이긴다 — 둘 다 참이다

| | Scratch | Center | Loc | Edge-Loc | Edge-Ring |
|---|---:|---:|---:|---:|---:|
| 대조 균일/smooth7_max | 0.7586 | 0.7455 | 0.9150 | **0.9844** | **0.9692** |
| T-RADIAL/smooth7_max | **0.8111** | **0.7495** | **0.9477** | 0.9666 | 0.9375 |
| T-RADIAL/smooth3_max | 0.8090 | **0.8558** | **0.9702** | 0.9358 | 0.9006 |

전체 AUPR 은 Edge 계열이 지배한다(결함 7,894 중 3,898장 = 49.4%).
template 이 Edge 를 깎고 Scratch/Loc 을 올리니 **합계는 지고 값어치 남은 곳에서는 이긴다.**
**하나만 쓰면 거짓말이 된다.**

## 정정 4 — 등가성을 argsort 로 박으려다 실패했다

"국소 밀도 = 균일 template 잔차 뭉개기" 를 처음에 `argsort` 결과가 같다로 박으려 했는데
테스트가 깨졌다. 항등식 오차는 8e-16 인데 **아핀 변환이 1 ULP 차이를 없애거나 만들어
동점 구조가 달라진다**(동점 3개 vs 4개). 그러면 순열이 달라지고 AUROC 도
0.5006 대 0.5019 로 갈린다. **등식 자체와 기울기 부호를 박고,
값이 실제로 다른 쌍의 대소만 비교하는 것**으로 고쳤다.

## 국소화 산출물 (범위 확장 대응)

- `result/ood/o1_maps/o1_maps_pad.npz` — 결함 7,894 전부 + 정상 5,000, **원 좌표**.
  `density_k5`(template 없음)와 `radial_residual_k5`(template 보정) **둘 다** 저장.
  웨이퍼 id(`cache_index`), 라벨, `die_size`, `lot_name` 동반.
  어느 맵이 하류 8종 분류를 돕는지 **모르기 때문에** 둘 다 남겼다.
- `docs/research/ood_template/evidence/cases_{scratch,center,loc,edgering}.png` —
  잘 잡은 것 / **놓친 것** / 헛경보 정상 / 전형적 정상.

**픽셀 단위 정답은 만들지 않았다**(기획서 §9.3). 데이터에 다이 단위 마스크가 없다.

정성 관찰 세 가지:
1. 잡은 Scratch 에서 **선이 맵에 그대로 보인다.** 국소화가 실제로 된다.
2. 놓친 Scratch 는 불량 비율 0.016~0.048 이다. **선이 창보다 가늘어** 5x5 창 안 밀도가
   0.23 밖에 안 된다. 놓치는 이유가 명확하다.
3. 헛경보 정상은 **가장자리 링이 켜진다.** 반경 template 이 그 자리를 깎아 주는 것이
   그림에서도 보인다.

## 왜 밀도로도 한계가 있는가 — 데이터가 정한 천장

`none` 은 "불량 다이가 없다" 가 아니라 **"알아볼 만한 패턴이 없다"** 이다.

| | 중앙값 | 하위 10% |
|---|---:|---:|
| none (n=110,701) | 0.0883 | 0.0404 |
| Scratch (n=693) | **0.1037** | 0.0576 |

**Scratch 의 불량 다이 비율 중앙값이 정상의 1.17배다.**
불량률 0.10 초과가 정상 43,923장 대 결함 6,225장이다. 정상 절대 수가 7배 많다.

## 만든 것

- `src/a22_ood_template.py` + `tests/test_a22_ood_template.py` (13)
- `src/a23_ood_template_eval.py` + `tests/test_a23_ood_template_eval.py` (9),
  `tests/test_a23_paired.py` (4)
- `src/a24_ood_residual.py` + `tests/test_a24_ood_residual.py` (13),
  `tests/test_a24_local_density_pin.py` (22)
- `src/a25_ood_residual_eval.py`, `src/a26_ood_residual_export.py` +
  `tests/test_a26_ood_export.py` (9)
- `src/diag_ood_paired.py`, `src/diag_ood_density_ceiling.py`,
  `src/diag_ood_local_density_control.py`

## 다음

1. **새 바닥 0.7147 위에서 O2/O3 를 다시 설계한다.** 이전 사다리의 채택 기준은 무효다.
2. 놓치는 것이 **창보다 가는 구조**라는 게 확인됐다. 방향성 필터, 여러 창 크기 결합.
3. 두 맵 중 무엇이 하류 8종 분류를 돕는지 — MLLM 사이클의 간접 평가(§9.4).

---

# 3차 사이클 — 창 모양을 결함 모양에 맞춰 봤고, 정상 웨이퍼에 대해 알게 됐다 (2026-08-23 00:15~01:00)

## 무엇을 왜 했는가

2차 사이클의 정성 관찰에서 **놓치는 이유가 보였다**: 놓친 Scratch 는 선이 창보다 가늘다.
7x7 창에 길이 7 짜리 1픽셀 선이 들어오면 밀도가 7/49 = 0.14 인데
같은 창의 5x5 덩어리는 20/49 = 0.41 이다. **정사각 창이 선을 조직적으로 희석한다.**

→ 창을 선 모양으로 바꾸면 같은 선이 1.0 이 된다. 학습이 아니라
**결함 모양에 대한 사전지식을 창 모양에 넣는 것**이라 여전히 train 자료를 안 쓴다.

**주 arm 을 하나로 미리 고정했다**(`line7_8dir`: 길이 7, 폭 1, 8방향, 방향과 위치에 대해 최대).
변형을 여러 개 재고 최고를 고르면 test 로 고른 것이라 누수이기 때문이다.
나머지 7개는 탐색용으로 표시하고 판정에 쓰지 않았다.

GPU 0번은 계속 9-class 쪽이 쓰고 있어 **이번에도 CPU 만 썼다.**

## 주 arm 은 참담하게 실패했다

**AUPR 0.0804** — 유병률 0.0666 바로 위다. 대조 정사각 k=7(0.7145) 대비
짝지은 차이 **[-0.6430, -0.6256], p=0.0000**. Scratch 도 0.7586 → 0.6332 로 떨어졌다.
반증 조건 1, 2, 4, 5 를 전부 위반해 기각.

## 왜 — 포화

클래스별 AUROC 에서 **다섯 클래스가 정확히 같은 값(0.6490)** 이 나온 게 단서였다.
점수의 **고유값이 13개뿐**이다.

**정상 웨이퍼의 70.21% 가 어딘가에 불량 7칸짜리 직선을 갖고 있다.**
창이 포화하면 점수가 상수가 되고 순위가 무너진다.
길이별로: line5 정상 포화 95.24%, line7 70.21%, line9 54.72%, line11 50.73%,
정사각 k=7 은 **0.00%**.

### 첫 설명이 양적으로 틀렸다 (자기 정정)

"창 후보가 8방향 x 수천 위치라 다중비교로 우연히 걸린다" 고 읽었는데 **계산이 안 맞는다.**
독립 Bernoulli 아래 기대 포화 창 수는 `8 x 800 x 0.116^7 = 0.002`,
즉 **0.2% 여야 하는데 70% 다. 350배 차이다.**
우연이 아니라 **정상 웨이퍼의 불량이 실제로 뭉쳐 있다.**

### 어디에 뭉쳐 있는가 — 가장자리 링

포화된 창의 정규화 반경을 재니 **정상 포화의 95.49% 가 반경 0.85 바깥**이었다.
기작 확인용으로(사전등록 arm 아님) 안쪽만 보게 하니
정상 포화 70.63% → 19.19%, AUROC 0.6371 → 0.8047 로 움직였다.
**원인이 가장자리 링이라는 것이 확인된다.**

**→ 새 사실: 정상 웨이퍼의 바깥 링에는 연속으로 죽은 다이가 줄줄이 있다.**
왜 그런지는 모른다.

## 예측 채점 — 기작은 맞혔고 파급은 못 맞혔다

실행 전에 "정상 웨이퍼의 가장자리 링을 선 창이 따라가 정상에서도 밀도 1.0 이 쉽게 나올 것이고,
그래서 Edge 가 나빠질 것" 이라고 적었다. **기작은 정확히 맞았다.**
그런데 "Scratch 는 오른다" 는 틀렸고, 파급도 과소평가했다 —
정상 쪽이 천장에 붙으면 Edge 만이 아니라 **순위 전체**가 무너진다.

2차 사이클에서는 예측이 4개 중 3개 틀렸고 이번에는 기작을 맞혔다.
**실행 전 예측을 적어 두는 것이 교정 기록으로 값어치를 내기 시작했다.**

## 탐색 arm — 판정에 안 쓰지만 기록한다

### 다중척도 결합이 대조군을 이겼다 (미확인)

k=3,5,7,9 정사각 밀도를 train-none 백분위로 바꿔 Fisher 결합:
**AUPR 0.7264 [0.7176, 0.7346]**, 짝지은 차이 **[+0.0096, +0.0142], p=0.0000**,
**FPR@95TPR 0.4295**(대조군 0.4844).
**Center 가 0.7458 [0.7285,0.7624] → 0.7971 [0.7814,0.8116] 로 CI 가 안 겹치게 오른다.**
Scratch 는 0.7586 → 0.7451 로 조금 나빠진다(CI 겹침).

**선택 위험을 명시한다**: 탐색 arm 7개 중 1개가 이겼고 **무엇을 부각할지는 결과를 보고 정했다.**
효과 크기(+0.012)도 k=3 대 k=7 차이(+0.216)에 비하면 작다.
**주 arm 으로 사전등록해 다시 재기 전에는 채택하지 않는다.**

### 중심 다이 제외는 Center 를 돕지 않았다 — 내 가설이 틀렸다

정상의 중심 최근접 다이가 58.9% 불량이라 빼면 Center 가 좋아질 줄 알았는데
**오히려 조금 나빠졌다**(Center 0.7458 → 0.7380, 전체 0.7145 → 0.7135).
중심 한 칸은 7x7 창 49칸 중 하나라 밀도에 거의 영향이 없다.
**중심 다이 교란은 셀 하나짜리 통계(반경 template)에서만 문제였고 창 통계에서는 아니다.**

### 창 셀 수가 shape 보다 중요하다 — 다만 shape 도 남는다

셀 수 순으로 대체로 좋아진다(5칸 0.0658 → 49칸 0.7145). 밀도 추정의 표본 크기라서다.
그런데 **정사각 k=3(9칸) 0.4986 과 line9(9칸) 0.0943 은 같은 셀 수인데 5배 넘게 갈린다.**
차이는 **최대를 취하는 후보 집합의 크기**다 — 정사각은 방향 1개, 선은 8개라
정상도 8배 많은 기회를 갖는다. **`max` 뭉개기는 창 모양뿐 아니라 후보 수에도 민감하다.**

## 만든 것

- `src/a27_line_filter.py` + `tests/test_a27_line_filter.py` (19)
- `src/a28_line_filter_eval.py`, `src/diag_ood_line_saturation.py`
- `docs/research/ood_line/evidence/{line_filter_run.txt, line_saturation.txt}`

구현 중 잡은 버그 하나: 처음에 커널을 촘촘히(4*length 점) 이산화했더니
22.5° 커널이 9칸이 되고 축 방향은 7칸이었다. **방향마다 창 크기가 달라
비스듬한 긁힘만 더 희석되는** 구조였다 — 이 필터를 만든 이유 자체를 무너뜨린다.
정수 t 로만 뽑아 모든 방향을 7칸으로 맞췄고 테스트로 박았다.

## 다음

1. **다중척도 결합을 주 arm 으로 사전등록해 재확인한다.**
2. 포화는 `max` 뭉개기의 성질이다. 귀무를 무작위 배치로 두면 너무 느슨하다
   (정상의 뭉침이 진짜이므로). **정상 웨이퍼 자체를 귀무로 쓰는 보정**이 맞는 방향이다.
3. **가장자리를 따로 다뤄야 한다.** 정상에서도 링이 포화한다.

---

# 4차 사이클 — 정상 웨이퍼 자체를 귀무로 쓴다 (2026-08-23 00:50~01:20)

## 무엇을 왜 했는가

3차 사이클이 남긴 사실 하나로 곧장 갔다.

> 정상 웨이퍼의 바깥 링에는 연속으로 죽은 다이가 줄줄이 있고,
> 포화의 95.49% 가 반경 0.85 바깥이다.

즉 **밀도 0.6 의 뜻이 안쪽과 가장자리에서 다르다.** 가장자리에서는 흔하고 안쪽에서는 이상하다.
그런데 채점기는 위치를 안 보고 밀도의 최대만 취한다 — 가장자리가 늘 이기고 헛경보가 거기서 난다.

→ 각 다이의 밀도를 **같은 정규화 반경 대역에 있는 train-none 다이들의 경험분포에서의
백분위**로 바꾼다. "이 자리 치고 얼마나 드문가" 가 된다.
3차 사이클 마지막에 "무작위 배치 귀무는 너무 느슨하다, 정상 웨이퍼 자체를 귀무로 쓰는 편이
맞다" 고 적은 것의 실행이다. GPU 는 이번에도 안 썼다.

**대조군을 두 개 미리 넣었다** — 보정 없음, 그리고 **전역 보정**(대역을 안 나눈 하나의 분포).
전역 보정이 없으면 "보정" 의 효과와 "반경 대역" 의 효과를 못 가른다.
2차 사이클에서 대조군을 빠뜨려 결론이 뒤집힌 적이 있어서다.

## 결과 — 사전등록한 주 arm 이 처음으로 통과했다

| arm | AUROC | AUPR | AUPR 95% CI | FPR@95TPR |
|---|---:|---:|---:|---:|
| 대조 보정없음 k=7 | 0.9207 | 0.7145 | [0.7058, 0.7231] | 0.4844 |
| 대조 전역보정 k=7 | 0.9207 | 0.7121 | [0.7036, 0.7217] | 0.4855 |
| **주 radial_cal_k7 (32대역)** | **0.9396** | **0.7407** | [0.7313, 0.7499] | **0.3603** |

- vs 보정 없음 **[+0.0217, +0.0307], p=0.0000**
- vs 전역 보정 **[+0.0240, +0.0331], p=0.0000**
- **전역 보정 단독은 보정 없음보다 오히려 조금 나쁘다**([-0.0034, -0.0016], p=0.0000)

**이득의 출처가 특정된다: 백분위로 바꾼 것이 아니라 반경 대역별로 바꾼 것이다.**
3차 사이클에서 밝힌 기작이 그대로 확인된다.

### Scratch, Center, Loc 을 동시에 최고로 만든 첫 arm

| | Scratch | Center | Loc |
|---|---|---|---|
| 대조 보정없음 k=7 | 0.7586 [0.7410, 0.7726] | 0.7458 [0.7285, 0.7624] | 0.9151 [0.9078, 0.9210] |
| **주 radial_cal_k7** | **0.8184 [0.8034, 0.8309]** | **0.8112 [0.7954, 0.8263]** | **0.9529 [0.9470, 0.9577]** |

셋 다 CI 가 겹치지 않는다. FPR@95TPR 도 0.4844 → 0.3603 —
결함 95% 를 잡을 때 같이 띄우는 정상이 **53,600장에서 39,900장으로** 준다.

dieSize 구간별로도 대부분 개선되는데 **`>1600` 만 0.5458 → 0.5244 로 나빠진다.**
조건 5 의 대상 구간은 아니지만 숨기지 않는다. 이유는 모른다.

## 이번엔 결과를 믿기 전에 먼저 의심했다

3차 사이클에서 `max` 뭉개기의 포화에 데었으므로 같은 검사를 먼저 돌렸다.

- **고유값 2,343개**(보정 없음은 574개). **정상 쪽 포화 0.06%** — 붕괴 없음.
- 다만 **결함의 31% 가 천장에 붙는다.** 다이 ~800개의 백분위 최대는 귀무에서도
  1 - 1/800 근처라 점수가 1 근처에 몰린다(정상 90분위가 이미 0.99472).
  순위 지표에는 문제가 없지만 **문턱을 정하기에는 나쁜 척도다.**
- 동점을 무작위로 20번 해소해 다시 재니 AUPR 0.7416~0.7445, **폭 0.0029.**
  효과 크기(+0.0262)의 1/9 이라 판정이 안 뒤집힌다.
  덧붙여 보고값 0.7407 이 그 범위보다 **약간 낮아 보수적**이다.

## 실행 전 예측 — 네 개 다 맞았다

FPR@95TPR 하락 / Scratch·Loc·Center 상승 / **Edge-Ring·Edge-Loc 하락(원리적 대가)** /
전체 AUPR 은 불확실. 넷 다 맞았고 전체는 올랐다(Edge 손실이 작았다).

2차 사이클 4개 중 3개 오답 → 3차 기작만 적중 → **4차 전부 적중.**
**기작을 데이터에서 먼저 확인하고 나서 예측하면 맞는다**는 것이 세 사이클에 걸쳐 보인다.

## 국소화에서 나온 새 사실 — 가장 좋은 탐지기가 가장 나쁜 지도를 만든다

보정된 맵을 그림에 넣었더니 **온통 노랬다.** 위의 "1 근처에 몰린다" 가 그림으로 나타난 것이다.
`-log10(1-u)` 로 펴서 다시 그리니 **원시 밀도 맵보다 오히려 선명했다** —
배경이 눌리고 긁힘만 남는다.

**탐지 성능과 국소화 가독성은 다른 축이다.** 같은 값이라도
어떤 척도로 보느냐에 따라 하류가 쓸 수 있는지가 갈린다.
저장은 백분위 u 로 하되(무손실) **표시와 하류 입력은 surprisal 척도**로 쓰는 것이 맞다.

## 만든 것

- `src/a29_radial_calibration.py` + `tests/test_a29_radial_calibration.py` (10)
- `src/a30_radial_calibration_eval.py`
- `src/a26_ood_residual_export.py` 갱신 — 맵 3종 저장, 점수를 보정 밀도로 교체,
  보정 맵은 surprisal 척도로 표시
- `docs/research/ood_radialcal/evidence/radial_calibration_run.txt`

## 다음

1. **점수 척도를 고친다.** 백분위 최대 대신 다이별 `-log(1-u)` 의 합이나 상위 k번째 값.
   문턱 설정에 낫고 천장 동점도 줄어든다. 사전등록 후 확인.
2. 3차의 **다중척도 결합**과 직교할 수 있다. 결합 후보.
3. `>1600` 구간 회귀의 원인.

## 운영 지점 표를 상설로 만들었다

**AUPR 총합은 어디서 무너지는지를 가린다.** `a21_ood_metrics.operating_points` 를 TDD 로 추가하고
보고서에 상설로 넣기로 했다. 팹이 감당하는 것은 곡선 아래 넓이가 아니라 헛경보 장수다.

| arm | 재현율 50% | 재현율 80% | 재현율 95% |
|---|---|---|---|
| | precision / 헛경보 | precision / 헛경보 | precision / 헛경보 |
| E0 불량 다이 비율 | 0.247 / 12,024 | 0.121 / 46,016 | 0.084 / 81,522 |
| 국소 밀도 k=3 | 0.486 / 4,174 | 0.278 / 16,414 | **0.133** / 48,894 |
| 국소 밀도 k=5 | 0.791 / 1,043 | **0.364** / 11,059 | 0.133 / 48,927 |
| 국소 밀도 k=7 | **0.863** / 628 | 0.356 / 11,402 | 0.123 / 53,621 |
| 국소 밀도 k=9 | 0.839 / 759 | 0.327 / 12,980 | 0.116 / 56,933 |
| **반경보정 밀도 k=7** | **0.880 / 540** | **0.464 / 7,290** | **0.158 / 39,889** |

**50% → 80% 사이에서 precision 이 반토막 난다**(k=7: 0.863 → 0.356).
반경 보정이 그 구간을 가장 크게 올린다(0.356 → 0.464, 헛경보 11,402 → 7,290).

**창 크기는 운영 지점마다 우열이 갈린다 — 하나가 지배하지 않는다.**
50% 에서는 k=7, 80% 에서는 k=5, 95% 에서는 k=3 이 최고 precision 이다.
**단일 최적 k 를 고르면 안 된다.**

## 발견한 문제 — 다음 사이클로 넘긴다

운영 지점을 재면서 동점 민감도를 같이 쟀는데 **k=3 에서 심각하다.**

| arm | 고유값 | 결정론적 AUPR | 동점 무작위 해소 AUPR |
|---|---:|---:|---|
| 국소 밀도 k=3 | 108 | **0.4986** | **[0.4112, 0.4248]** |
| 국소 밀도 k=5 | 875 | 0.6876 | [0.6819, 0.6856] |
| 국소 밀도 k=7 | 2,017 | 0.7145 | [0.7144, 0.7151] |
| E0 불량 다이 비율 | 7,986 | 0.3856 | [0.3857, 0.3857] |

**k=3 의 결정론적 값이 무작위 해소 범위 바깥에 있다.** 우연이 아니라 편향이다 —
동점 안에서 원래 인덱스 순서를 쓰는데, test 배열 위치와 결함 여부에 약한 상관이 있다
(전체 0.0116, 3사분위 결함비율 0.0967 대 나머지 약 0.055).
즉 **동점 처리를 통해 라벨 정보가 샌다.**

조정자가 준 k=3 AUPR 0.3845 와 내 0.4986 이 다른 이유가 이것으로 설명된다.
**둘 다 각자의 관례에서는 맞다.** 그렇다면 그 지점의 AUPR 은 관례를 밝히지 않으면
정의되지 않는 값이다. **5차 사이클에서 동점을 블록으로 묶는 관례로 고친다.**

덤으로 하나 더: 점수를 npz 에 **float32 로 저장**하면 동점이 새로 생긴다.
같은 k=3 점수가 메모리에서는 고유값 108개인데 float32 로 저장했다 읽으면 24개다.
**저장 후 다시 잰 지표와 메모리에서 잰 지표가 다를 수 있다.**

---

# 5차 사이클 — 측정 도구가 만든 신호를 걷어내고, 분모를 바꿔 봤다 (2026-08-23 01:20~02:00)

## 시작은 숫자가 안 맞아서였다

조정자가 재확인한 k=3 국소 밀도 AUPR 이 **0.3845** 인데 내 기록은 **0.4986** 이었다.
0.11 차이는 무시할 수 없어서 원인을 팠고, **내 쪽이 틀렸다.**

## 정정 A — `uniform_filter` 가 동점을 쪼갰고, 그 가짜 구분이 라벨을 흘렸다

창 3x3 밀도가 가질 수 있는 값은 **분모 9 이하의 유리수 23개**뿐이다.
그런데 `scipy.ndimage.uniform_filter` 로 창합을 구하니 **96~108개**가 나왔다.
분리 가능 running sum 이 평균을 구하며 마지막 비트에 오차를 쌓아
**같아야 할 값들이 1e-16 수준에서 갈린 것**이다. 음수 밀도(-1.33e-16)까지 나왔다.

그 가짜 구분이 동점을 갈랐고, 동점은 원래 인덱스 순서로 정렬되는데
**test 배열 위치가 결함 여부와 약하게 상관돼 있다**(전체 0.0116,
3사분위 결함비율 0.0967 대 나머지 약 0.055). 즉 **측정 도구의 반올림 잡음이
라벨 정보로 둔갑했다.**

정수 창합(`window_sum`)으로 고치니 고유값 23개, AUPR **0.3844** — 조정자 값과 일치한다.

| k | 고친 뒤 고유값 | 고치기 전 | AUPR 변화 |
|---|---:|---:|---|
| 3 | 23~24 | 96~108 | 0.4638 → 0.3495 (표본), 0.4986 → 0.3844 (전수) |
| 5 | 160~182 | 626~875 | 0.6575 → 0.6406 |
| 7 | 574 | 2,017 | 0.7145 → 0.7129 |

**k>=5 에서는 0.002 이하라 다른 결론은 안 흔들린다.** k=3 만 심각했다.

## 정정 B — 주 지표를 blocked AUPR 로 바꿨다

같은 점수를 받은 표본은 **어떤 문턱으로도 서로 갈리지 않는다.** 그러니 지표도 갈라선 안 된다.
서로 다른 문턱 값에서만 곡선 위의 점을 찍는 `aupr_blocked` 를 TDD 로 추가했다.
**입력 순열에 대해 폭이 정확히 0** 이다(기존 AUPR 은 k=3 에서 폭 0.0036).
정보 없는 점수에서는 정확히 유병률이 나온다.

바닥 수치를 다시 박았다(`tests/test_a24_local_density_pin.py`).

## 그 다음 — 진단 도중에 우연히 본 것

원인을 찾다가 분모를 바꾼 변형(`k2`: 창 안 다이 개수 대신 **창 넓이 k²** 로 나눔)을
곁다리로 재봤는데 값이 크게 나왔다. **사후 관찰이므로 사전등록하고 전면 평가를 새로 돌렸다.**

가장자리에서는 창의 일부가 웨이퍼 밖이라 `k2` 가 저절로 작아진다 —
**4차 사이클의 반경 보정이 정상 통계로 하던 일을 분모 선택만으로, 학습 없이 흉내내는 셈**이다.

## 결과 — 축은 기각, 합성은 새 최선

| arm | AUROC | **AUPR블록** | FPR@95TPR | 고유값 |
|---|---:|---:|---:|---:|
| **합성 k2 k=5 + 반경보정** | 0.9497 | **0.7646** [0.7558, 0.7737] | 0.3226 | 283 |
| 주 k2 k=5 | 0.9515 | 0.7416 | **0.3103** | **25** |
| 4차 챔피언 (반경보정 die k=7) | 0.9396 | 0.7412 | 0.3603 | 2,267 |
| die k=7 | 0.9206 | 0.7098 | 0.4854 | 574 |

**조건 1 기각**: `k2` 가 k=3,5 에서는 크게 이기지만 k=7,9 에서는 **진다**(2/4).
"분모를 창 넓이로 바꾸면 좋아진다" 는 일반 주장은 성립하지 않는다 —
분모는 **창 크기와 상호작용**한다.

**조건 2 무승부**: 주 arm 단독은 챔피언과 구분되지 않는다([-0.0046, +0.0054], p=0.9100).

**조건 7 통과**: 합성이 두 구성 요소를 다 이긴다(각 p=0.0000).
**둘은 같은 일을 하지 않는다 — 내 예측이 틀렸다.**

## 정정 C — 검정을 주 지표에 맞추지 않았다, 그리고 그게 판정을 뒤집었다

`a31_window_norm_eval.py` 의 짝지은 검정이 **순서 의존 AUPR** 을 썼다.
후보 문서에 "모든 판정은 blocked 로 한다" 고 적어 놓고서다.

| 비교 | 순서 의존 | blocked (주 지표) |
|---|---|---|
| 주 k2 k=5 vs 챔피언 | [+0.0126, +0.0240] p=0.0000 **이김** | [-0.0046, +0.0054] p=0.9100 **무승부** |

**순서 의존 검정만 봤으면 새 챔피언을 잘못 선언했을 것이다.**
`paired_aupr_blocked_diff_ci` 를 TDD 로 추가하고 판정을 다시 냈다.
2차의 "뭉개기를 바꾸면 대조군도 바꿔야 한다" 와 같은 종류의 실수다 —
**한 축을 바꾸면 그것에 딸린 것들을 같이 바꿔야 한다.**

## 클래스별 — Center 가 사실상 풀렸다

| | Scratch | Center | Loc |
|---|---|---|---|
| E0 개수 (원래 바닥) | 0.6559 | 0.7540 | 0.8095 |
| 4차 챔피언 | 0.8183 | 0.8108 | 0.9528 |
| **주 k2 k=5** | **0.8471** | **0.9315** | **0.9795** |
| 합성 | 0.8409 | 0.8782 | 0.9772 |
| **k2 k=3** | 0.8199 | **0.9612** | 0.9784 |

**Center 0.7540 → 0.9612.** 이 워크스트림에서 가장 오래 막혀 있던 클래스다.
**이제 남은 것은 Scratch(0.85) 하나다.**

## 운영 지점 — 붕괴 구간이 올라왔다

| arm | 50% | 80% | 95% |
|---|---|---|---|
| E0 불량 다이 비율 | 0.247 / 12,024 | 0.121 / 46,016 | 0.084 / 81,522 |
| die k=7 | 0.861 / 639 | 0.355 / 11,465 | 0.122 / 53,729 |
| **합성** | **0.895 / 465** | **0.539 / 5,394** | 0.174 / 35,709 |

**80% 재현율에서 precision 0.355 → 0.539, 헛경보 11,465 → 5,394장.**

## 조건 6 이 주 arm 을 떨어뜨렸다 — 고유값

**주 k2 k=5 는 고유값이 25개뿐이다.** 분자가 정수라 원리적으로 26개가 최대다.
95% 재현율 문턱에서 **18,790장이 동점**이라 문턱 하나가 그만큼을 한꺼번에 넘긴다.
**실무에서 운영 지점을 고를 수 없다.** 합성은 283개(동점 968장)로 훨씬 낫다.
**AUPR 만 보고 k2 k=5 를 고르면 안 된다** — 그래서 합성을 최선으로 삼는다.

## 예측 채점

작은 k 일수록 `k2` 이득이 크다(**맞음**), Edge-Ring 이 나빠진다(**맞음**),
고유값이 적어 운영 지점이 듬성듬성하다(**맞음**),
`k2` 가 `die` 를 이긴다(**부분 오답**), `k2` 와 반경 보정이 겹친다(**틀림**).

## 만든 것

- `a21_ood_metrics.aupr_blocked`, `operating_points` + 테스트 2파일
- `a24_ood_residual.window_sum`, `local_fail_count_map/max` + `tests/test_a24_exact_density.py`
- `a23_ood_template_eval.paired_aupr_blocked_diff_ci` + 테스트
- `src/a31_window_norm_eval.py`, `src/diag_ood_windownorm_blocked.py`,
  `src/diag_ood_tie_convention.py`, `src/report_ood_operating_points.py`
- 바닥 재고정: `tests/test_a24_local_density_pin.py`

## 다음

1. `k2` 의 고유값 문제 — 다중척도로 섞으면 해상도가 올라갈 수 있다.
2. **Scratch 가 마지막 남은 클래스다**(0.85).
3. GPU 0번은 여전히 9-class 쪽이 쓰고 있다(e18 + 새로 뜬 e20). O2 는 비면 간다.

---

# 6차 사이클 — 기각했던 것을 되살렸다, 기작이 틀렸었기 때문에 (2026-08-23 02:00~02:40)

## 시작은 옆 워크스트림이 내 실수를 드러낸 것이었다

3차 사이클에서 방향성 선형 필터를 **포화 때문에** 기각하면서 원인을
**"정상 웨이퍼의 바깥 링에 연속으로 죽은 다이가 줄줄이 있다"** 로 적었고,
그것을 `docs/reports/OOD.md` 에 **"알아낸 사실"** 로 올렸다.

옆 워크스트림(`Semi-AD`)이 같은 필터를 이식하면서 포화 창의 다이 개수를 세었고
최빈값이 3이라고 기록했다. **내 worktree 에서 직접 확인했다**(표본 8,000장):

| 포화 창의 다이 개수 | 비중 |
|---:|---:|
| 3개 | **43.1%** |
| 4개 | 30.1% |
| 2개 | 17.2% |
| 1개 | 6.0% |
| **7개 (온전)** | **0.2%** |

**온전 창만 세면 정상 포화가 70.63% → 0.15% 다.**

포화는 링에 진짜 긴 불량 줄이 있어서가 아니라
**창이 웨이퍼 경계에서 잘려 분모가 2~4로 무너지기 때문**이다.
불량률 11.6% 에서 2~4개 연속은 우연히 늘 생긴다.

## 정정 — 틀린 분모 가정이 틀린 확률 계산을, 그게 틀린 기작을 낳았다

3차에서 "우연이 아니다" 라고 쓴 근거는 `8 x 800 x 0.116^7 = 0.002` 였다.
**그 계산이 창에 다이가 7개 있다고 가정했다.** 다이가 3개면 `0.116³ ≈ 0.0016` 이고
창 수를 곱하면 흔한 일이다. **계산이 무효이므로 "우연이 아니다" 라는 결론도 무효다.**

**틀린 분모 → 틀린 확률 → 틀린 기작 → 사실이 아닌 "사실".**
5차의 정정 8(도구가 만든 신호)과 같은 뿌리다 — **분모를 안 봤다.**

3차의 **기각 판정 자체는 유지된다**(그 구현으로는 정말 못 쓴다).
바뀌는 것은 **기각 사유**이고, 사유가 바뀌니 **고칠 방법이 생겼다.**

## 가드를 넣으니 뒤집혔다

`min_dies=length` — 창 안 다이가 length 개인 곳에서만 점수를 낸다.

| L | 가드 | 없음 | 짝지은 차이(blocked) | p |
|---|---:|---:|---:|---:|
| 5 | 0.2270 | 0.0696 | [+0.1535, +0.1612] | 0.0000 |
| 7 | 0.4826 | 0.0891 | [+0.3847, +0.4014] | 0.0000 |
| 9 | 0.6361 | 0.1039 | [+0.5226, +0.5415] | 0.0000 |
| 11 | 0.6562 | 0.1052 | [+0.5421, +0.5605] | 0.0000 |
| 15 | 0.6005 | 0.1012 | [+0.4907, +0.5083] | 0.0000 |

**5/5 통과.** 그리고 **Scratch AUROC 0.6332 → 0.9339(단독 L=9)** 다.

## 그러나 정사각 창에서는 가드가 해롭다 — 예측이 틀렸다

"분모 붕괴는 창 모양과 무관하니 정사각에도 통한다" 고 예측했다. **틀렸다.**
정사각 die k=7: 0.7098 → **0.6239**, [-0.0950, -0.0764], p=0.0000.

선 창은 길이 7 중 3칸만 남는 식으로 **심하게** 잘리지만,
정사각 7x7 은 가장자리에서도 20칸 안팎이 남아 분모가 그만큼 안 무너진다.
거기서 온전 창(49칸)을 요구하면 **가장자리를 통째로 버리는 손해**가 더 크다.
**가드는 창 모양에 따라 부호가 바뀐다.**

## 새 최선 — 융합

단독 선 필터는 최고가 0.6562 로 5차 최선(0.7646)에 **전부 진다.**
고유값이 11개뿐이라 운영 지점도 못 고른다. **그런데 융합하면 이긴다.**

| arm | AUROC | **AUPR블록** | FPR@95TPR | 고유값 |
|---|---:|---:|---:|---:|
| **융합 최선+line11 온전창** | 0.9583 | **0.8063** [0.7984, 0.8128] | 0.2551 | 1,410 |
| 융합 최선+line7 온전창 | **0.9616** | 0.7777 | **0.1776** | 916 |
| 5차 최선 (k² 5x5 + 반경보정) | 0.9497 | 0.7646 | 0.3226 | 283 |

vs 5차 최선: L=11 **[+0.0357, +0.0462]**, L=7 [+0.0071, +0.0188], 둘 다 p=0.0000.
**선 창과 정사각 창은 서로 다른 결함을 본다.**

## 운영 지점 — 원래 바닥 대비 12배

| arm | 50% | 80% | 95% |
|---|---|---|---|
| E0 불량 다이 비율 (원래 바닥) | 0.247 / 12,024 | 0.121 / 46,016 | 0.084 / 81,522 |
| 정사각 die k=7 | 0.861 / 639 | 0.355 / 11,465 | 0.122 / 53,729 |
| **융합 L=11** | **0.932 / 287** | **0.628 / 3,734** | 0.210 / 28,245 |
| 융합 L=7 | 0.886 / 510 | 0.598 / 4,245 | **0.276 / 19,666** |

**80% 재현율에서 헛경보 46,016 → 3,734장, precision 0.121 → 0.628.**
95% 에서는 L=7 융합이 19,666장으로 가장 낫다 — **운영 지점마다 최선이 갈리므로 둘 다 낸다.**

## 클래스별 — Scratch 가 풀렸고 Center 가 남았다

| | Scratch | Center |
|---|---|---|
| E0 개수 (원래 바닥) | 0.6559 | 0.7540 |
| 3차 line7 가드없음 | 0.6332 | 0.5733 |
| 5차 최선 | 0.8409 | 0.8782 |
| **융합 L=11** | **0.9355 [0.9268, 0.9455]** | 0.8654 |
| k² 3x3 단독 | 0.8199 | **0.9612** |

**"선 창이 가는 긁힘을 잡는다" 는 주장은 참이었다.**
3차에서 기각한 것은 주장이 틀려서가 아니라 **분모가 망가진 구현으로 쟀기 때문**이다.

**남은 것은 Center 다.** 다만 `k² 3x3` 이 이미 0.9612 를 내므로 신호는 손에 있다.

## 조건 7 — 가드가 웨이퍼를 침묵시키지 않는다

점수가 0 이 되는 웨이퍼가 L 마다 3~4장이고 **그중 결함은 0장**이다.
118,595장 중 4장이라 지표에 영향이 없다. (예측은 "작은 웨이퍼가 손해를 본다" 였고 **틀렸다.**)

## 예측 채점

포화가 없어진다(**맞음**), Scratch 가 크게 오른다(**맞음**),
작은 웨이퍼가 손해를 본다(**틀림**), 정사각에도 통한다(**틀림**),
전체는 못 넘을 수 있다(단독은 **맞음**, 융합은 넘음).

## 만든 것

- `a27_line_filter` / `a24_ood_residual` 에 `min_dies` 가드 + `tests/test_a27_full_window.py` (9)
- `src/a32_full_window_eval.py`
- `docs/research/ood_fullwindow/evidence/full_window_run.txt`

## 다음

1. **`k² 3x3`(Center 0.9612)을 융합에 넣는다.** 남은 클래스가 Center 하나다.
2. GPU 0번은 여전히 9-class 쪽(e18 + e20)이 쓴다. O2 는 비면 간다.
3. 국소화 산출물을 새 최선에 맞춰 다시 낸다.

---

# 7차 사이클 — 세 신호가 서로 다른 결함을 본다 (2026-08-23 02:40~03:00)

## 무엇을 왜 했는가

6차 끝에 남은 그림이 이랬다.

| 신호 | 단독 Center | 단독 Scratch |
|---|---:|---:|
| A: k² 5x5 + 반경보정 | 0.8782 | 0.8409 |
| B: 온전창 선 L=11 | 0.8129 | 0.9293 |
| **C: k² 3x3** | **0.9612** | 0.8199 |
| A+B (6차 최선) | 0.8654 | 0.9355 |

**Center 를 잘 보는 신호(C)가 손에 있는데 융합에 안 들어가 있었다.**
A+B 의 Center 0.8654 가 C 단독 0.9612 보다 낮다는 것이 그 증거다.
그래서 C 를 세 번째 요소로 넣었다. 가중치는 동일 고정(test 로 고르면 누수다).

## 결과 — 반증 조건 전부 통과

| arm | AUROC | **AUPR블록** | FPR@95TPR | 고유값 |
|---|---:|---:|---:|---:|
| **주 fuse3 (A+B+C)** | **0.9698** | **0.8325** [0.8261, 0.8396] | **0.1719** | 3,953 |
| 탐색 B+C (A 없음) | 0.9682 | 0.8135 | 0.1887 | 71 |
| 대조 A+B (6차 최선) | 0.9583 | 0.8063 | 0.2551 | 1,410 |
| 대조 A+C (선 없음) | 0.9596 | 0.7939 | 0.2432 | 1,026 |

- vs A+B **[+0.0233, +0.0290]**, vs A+C **[+0.0355, +0.0419]**, 둘 다 p=0.0000
- **Center 0.8654 [0.8531,0.8801] → 0.9412 [0.9328,0.9496]** — CI 안 겹침, 목적 달성
- **Scratch 0.9355 → 0.9251** — CI 는 겹치지만 점추정이 0.010 내려갔다. **Center 를 얻으며 치른 값이다.**

**그리고 A+C 는 A+B 보다 나쁘다**([-0.0181, -0.0074], p=0.0000).
C 가 단독으로는 B 보다 AUPR 이 높은데(0.6850 대 0.6562) A 와 붙이면 뒤집힌다.
**상보성의 방향을 미리 아는 법을 나는 모른다.**

## 운영 지점 — 원래 바닥의 1/16.6

| arm | 50% | 80% | 95% |
|---|---|---|---|
| E0 불량 다이 비율 (원래 바닥) | 0.247 / 12,024 | 0.121 / 46,016 | 0.084 / 81,522 |
| A 단독 | 0.895 / 465 | 0.539 / 5,394 | 0.174 / 35,709 |
| A+B (6차) | 0.932 / 287 | 0.628 / 3,734 | 0.210 / 28,245 |
| **fuse3** | **0.947 / 223** | **0.695 / 2,778** | **0.283 / 19,027** |

dieSize 도 전 구간 최고다(>1600 0.5985).

## 탐색 — 반경 통계 없이도 거의 간다

`B+C` 만으로 blocked AUPR **0.8135** 다. 6차 최선(0.8063)보다 높고 주 arm 에만 진다.
**반경 대역 통계 없이 더 단순한 채점기가 가능하다는 뜻**인데,
고유값이 71개뿐이라 운영 지점 해상도가 나쁘다.
**탐색 arm 이므로 채택하지 않는다** — 주 arm 으로 사전등록해 다시 재야 한다.

## 예측 채점 — 다섯 개 다 맞았다

Center 가 0.90 안팎(0.9412), 전체 +0.01 안팎이고 +0.05 는 아님(+0.026),
Scratch 거의 불변(-0.010, CI 겹침), 고유값 증가(1,410 → 3,953),
Edge 계열 소폭 하락(0.9572 → 0.9540).

## 국소화 산출물을 새 최선에 맞춰 다시 냈다

`src/a34_export_pipeline_maps.py` (기존 `a26` 대체).
`result/ood/o1_pipeline_maps/pipeline_maps_pad.npz` —
결함 7,894 전부 + 정상 5,000, **원 좌표**, **세 요소 맵을 모두** 저장.

그림(`docs/research/ood_pipeline_maps/evidence/cases_*.png`)에서 보이는 것:
**B(선 필터) 맵이 긁힘 자국을 선명하게 그린다.** 배경이 눌리고 선만 남는다.
A 는 흩어진 불량에 넓게 반응하고, C 는 작은 덩어리에 반응한다.
**세 맵이 눈으로 봐도 다른 것을 본다** — 탐지에서 상보적이라는 수치와 그림이 일치한다.
놓친 Scratch(불량률 0.016~0.051)는 세 맵 다 어둡다 — 정말로 볼 것이 거의 없다.

## 지금 상태

**클래스별 병목이 사라졌다.** 최저가 Scratch 0.9251, Center 0.9412 다.
**다음 병목은 클래스가 아니라 운영 지점**이다 — 95% 재현율에서 아직 19,027장을 띄운다.

## 다음

1. `B+C`(반경 통계 없음)를 주 arm 으로 사전등록해 재확인.
2. 95% 재현율 구간이 남은 병목.
3. GPU 0번은 계속 9-class 쪽이 쓴다. O2 는 비면 간다.

---

# 8차 사이클 — 탐지 잘하는 신호가 종류도 아는가 (2026-08-23 03:00~03:25)

## 무엇을 왜 했는가

1단계(탐지)는 blocked AUPR 0.8325 까지 왔다. **다음 질문은 3단계에 무엇을 넘기느냐**다
(기획서 §9.4: MLLM 8종 분류). 2차 사이클에 단서가 있었다 — 구조 점수 S_llr 은
탐지에는 최악(Edge-Ring 0.023)인데 결함끼리 Edge-Ring 대 Scratch 를 0.983 으로 갈랐다.

그래서 **결함 웨이퍼끼리만** 놓고 8종 중 두 종을 고르는 **28쌍**을 각 신호 하나로 재봤다.
**분류기를 만들지 않는다.** 학습 0, 라벨은 평가에만. GPU 도 안 썼다(계속 9-class 쪽 점유).

## 결과 — 자명한 개수가 1위였다

| 신호 | 분리도 >= 0.80 인 쌍 | 평균 분리도 |
|---|---:|---:|
| **대조: 불량 다이 개수** | **18/28** | **0.8360** |
| 융합 fuse3 (탐지 최선) | 13/28 | 0.7900 |
| A: k² 5x5 + 반경보정 | 13/28 | 0.7746 |
| C: k² 3x3 | 12/28 | 0.7603 |
| B: 온전창 선 L=11 | 10/28 | 0.7453 |
| 파생 log(B)-log(C) (범위 B) | 7/28 | 0.7277 |

**조건 3 기각.** 대조군을 넘는 쌍이 A 7개, B 6개, C 6개, 융합 6개 — 과반(15)에 한참 못 미친다.
**세 신호를 합친 17쌍조차 대조군 단독 18쌍보다 적다.**

**이유가 있다.** 8종 중 많은 쌍이 **불량이 몇 개 났는가**로 갈린다
(Near-full 대 무엇이든, Random 대 Scratch). 개수는 그걸 그대로 잰다.
내 신호들은 **양과 크기를 정규화해 없앤 것들**이다 — 백분위, 비율, 창 안 밀도.
**정규화가 탐지를 좋게 만든 바로 그 성질이 클래스 판별에서는 정보를 버린다.**

**탐지 최선이 분류 최선이 아니다.** 이걸 모르고 3단계에 융합 점수만 넘겼으면 손해였다.

## 그래도 셋은 서로 다른 쌍을 가른다 (조건 2 통과)

| 쌍 | 강한 쌍 교집합/합집합 | Jaccard |
|---|---:|---:|
| A vs B | 7 / 16 | 0.44 |
| A vs C | 11 / 14 | 0.79 |
| **B vs C** | **5 / 17** | **0.29** |

**B 와 C 가 가장 안 겹친다** — 설계대로 선과 덩어리를 따로 본다.
**세 맵을 다 하류로 넘길 근거다.**

## 개수가 원리적으로 못 하는 것 — 그리고 범위 B 가 처음으로 값어치를 냈다

| 신호 | Scratch 대 Center 분리도 |
|---|---:|
| 대조 불량 다이 개수 | 0.5991 |
| 융합 fuse3 (탐지 최선) | 0.5845 |
| C: k² 3x3 | 0.8058 |
| **파생 log(B)-log(C)** | **0.9090** |

Scratch 와 Center 는 불량 개수가 비슷해서 **개수로는 거의 못 가른다.**
"긁힘은 선, Center 는 덩어리" 라는 **모양 사전지식을 명시적으로 넣은 특징 하나**가 0.909 를 낸다.
그리고 이 특징이 대조군을 넘는 쌍이 **10개로 전체 1위**다(A 7, B 6, C 6).

**기획서 §10 의 범위 B(결함 모양 사전지식)가 값어치를 낸 첫 사례다.**
대가도 분명하다 — 결함 모양을 미리 안다고 가정하므로 **A-arm 과 분리 보고**한다.

## 예측 채점

"개수도 꽤 잘 가른다"(**맞음, 예상보다 훨씬 강해 1위**),
"파생 B/C 가 Scratch 대 Center 에서 최강"(**맞음** 0.9090),
"상당수 쌍은 어떤 신호로도 안 갈린다"(**맞음**),
"B 가 Scratch 쌍을, C 가 Center 쌍을 잘 가른다"(**부분 오답** — B 의 상위가 오히려 `Center|X` 다).

## 3단계에 넘길 것 (이 사이클의 실질 산출물)

**하나로 줄이지 않는다. 어느 것도 지배하지 않는다.**

1. **불량 다이 개수** — 단일 최강 클래스 신호(18/28). 자명하지만 빼면 손해다.
2. **세 요소 맵 A, B, C** — 서로 다른 쌍을 가른다.
3. **파생 log(B)-log(C)** — 개수가 못 가르는 Scratch 대 Center 를 가른다. **범위 B.**

## 설명 없는 관측

- **왜 융합이 클래스 판별에서는 구성 요소보다 안 나은가.** 탐지에서는 셋을 합치면 올랐다.
  Fisher 결합이 "얼마나 이상한가" 한 축으로 접기 때문일 수 있으나 **검증 안 했다.**
- **B(선 필터)의 상위 쌍이 왜 `Center|X` 인가.** Center 가 선 점수를 낮게 받아서인데,
  그건 "선이 아님" 을 재는 것이지 "선임" 을 재는 게 아니다. 이 구분을 안 팠다.

## 다음

1. MLLM 사이클에 위 세 가지를 **다** 넘기고 간접 평가로 가린다.
2. 파생 특징을 더 만들 여지가 있다 — **범위 B 규칙**(사전지식 열거, test 로 튜닝 금지) 준수.
3. **탐지 최선이 곧 분류 최선이 아니라는 것**을 3단계 설계에서 다시 확인한다.

---

# 9차 사이클 — 나 자신을 감사한다 (2026-08-23 03:25~04:00)

## 무엇을 왜 했는가

이 워크스트림의 **구조적 약점**을 아직 안 쟀다.
**val 은 못 쓴다**(품질을 못 재고 파국만 감지 — 기획서 §1). 그래서 여덟 사이클 내내
**공식 test 에서 재고 그 수치를 보고 다음 방향을 정했다.**
반증 조건을 실행 전에 박고 주 arm 을 하나로 고정하는 규율은 지켰지만,
**"어떤 축을 다음에 팔지" 는 매번 test 결과를 보고 정했다.**
100개가 넘는 arm 을 재고 최고를 보고하고 있으니 **그 수치는 낙관적으로 편향돼 있다.**

lot 단위로 test 를 두 반쪽으로 갈라(lot 이름 해시) 같은 arm 을 다시 쟀다.
**아무것도 재적합하지 않는다** — 보정 참조는 train-none 그대로고 평가 집합만 바뀐다.
채택 계보 + **기각한 것 두 개**를 같이 넣었다.

## 결과 — 다섯 조건 전부 통과

| arm | 전체 | 반쪽1 | 반쪽2 | \|차이\| |
|---|---:|---:|---:|---:|
| **채택 fuse3** | 0.8325 | 0.8275 | 0.8370 | **0.0094** |
| 탐색 B+C | 0.8135 | 0.8083 | 0.8181 | 0.0099 |
| 채택 A+B (6차) | 0.8063 | 0.7971 | 0.8146 | 0.0174 |
| 채택 A 단독 (5차) | 0.7646 | 0.7580 | 0.7707 | 0.0127 |
| 채택 die k=7 (2차) | 0.7098 | 0.6938 | 0.7240 | 0.0301 |
| E0 비율 (원 바닥) | 0.3854 | 0.3669 | 0.4026 | 0.0357 |
| 기각 T-RESIZE S_nll | 0.3619 | 0.3465 | 0.3763 | 0.0298 |
| 기각 선 L=7 가드없음 | 0.0891 | 0.0876 | 0.0905 | 0.0029 |

- 두 반쪽의 **최고 arm 이 같다**(fuse3)
- **순위 Spearman = 1.0000** — 여덟 arm 순서가 완전히 같다
- 최고 arm 반쪽 차이 0.0094 < 1위-2위 격차 0.0190
- **바닥 대비 이득 0.4471 / 최대 반쪽 변동 0.0357 = 12.5배**
- **기각 arm 이 채택 arm 을 넘는 경우 없음** — 여덟 사이클의 기각 판정이 반쪽에서도 유지된다

## 예측 채점

큰 주장 안전(**맞음**), 기각은 반쪽에서도 유지(**맞음**), Spearman 0.95 이상(**맞음**, 1.0),
**"최상위 두세 개 순서가 뒤집힐 수 있다"(틀림 — 하나도 안 뒤집혔다)**.

## 이것으로 해결되지 않는 것 — 실행 전에 적어 둔 그대로

**반쪽 검사는 선택 편향을 없애지 못한다.** 두 반쪽 다 **내가 이미 본** test 다.
이 사이클이 준 것은 **"평가 집합을 절반으로 바꿔도 순서와 크기가 유지된다"** 하나뿐이고,
그건 "우연히 이 118,595장에만 맞는 arm 을 골랐다" 를 약화시키지만
**"test 전체에 조금씩 과적합됐다" 는 못 건드린다.**

**진짜 필요한 것은 한 번도 안 본 자료다** — 다른 fab, 다른 기간,
또는 **처음부터 lot 을 떼어 봉인한 홀드아웃**.
**이 워크스트림은 그걸 안 만들고 시작했다. 다음 워크스트림은 그것부터 만들어야 한다.**

## 설명 없는 관측

- **약한 arm 일수록 반쪽 변동이 크다**(원 바닥 0.0357, 최선 0.0094). 확인 안 했다.
- 반쪽2 가 거의 모든 arm 에서 높다. 결함비율이 조금 높아서(0.0674 대 0.0658)일 수 있으나
  분해 확인 안 했다.

## 밤 전체 마무리

`docs/reports/OOD.md` 맨 앞에 **전체 요약**을 넣었다 —
바닥이 틀렸던 것, 지금 어디까지 왔는지(운영 지점 표 포함), 통한 것/기각된 것/**정정 11건**,
국소화 산출물, 미완과 다음 단계.

---

# 10차 사이클 — 남은 오류의 정체 (2026-08-23 03:45~04:05)

## 무엇을 왜 했는가

클래스별 병목은 사라졌다(최저 Scratch 0.9251). **남은 병목은 운영 지점** —
결함 95% 를 잡으려면 아직 정상 19,027장을 띄운다.
그게 **모델이 못 가른 것**인지 **라벨이 원래 그런 것**인지 안 쟀다.
둘은 대응이 완전히 다르다.

**판정은 fuse3 점수를 쓰지 않고 내렸다** — 자기 점수로 자기 오류를 설명하면 순환이다.
문턱 결정에만 쓰고 집단 성격은 **불량 다이 비율**이라는 독립적인 자로 쟀다.

GPU 0번은 밤새 21.3/24.6 GB 를 9-class 쪽(e18 4시간 34분, e20)이 썼다. **CPU 만 썼다.**
`B+C` 확인은 **하지 않았다** — 결정론적이라 같은 수치가 나올 뿐이고
이미 그 수치를 본 뒤라 "확인" 이 되지 않는다. 미확인으로 표시만 유지한다.

## 결과

| 조건 | 값 | 판정 |
|---|---|---|
| 1. 헛경보 정상 중 참 결함 하위 25%(0.1060)보다 불량률 높은 비율 | **0.7804** (전체 정상은 0.3488) | 라벨 탓 |
| 2. 놓친 결함 중 정상 중앙값(0.0883)보다 불량률 낮은 비율 | **0.5140** | 볼 것 없음 |
| 3. 놓친 결함 클래스 분포 | Scratch/Center/Edge-Ring 이 0.22~0.26씩 | 퍼져 있음 |
| 4. 상위 10 lot 이 차지하는 헛경보 몫 | **0.0124** (4,025개 lot 에 분산) | 웨이퍼 단위 |

**헛경보로 걸린 정상의 78% 가 "참 결함 넷 중 하나보다 불량 다이가 많은" 웨이퍼다.**
전체 정상에서는 35% 이므로 채점기가 임의로 고른 게 아니다.
`none` 이 "불량이 없다" 가 아니라 **"알아볼 만한 패턴이 없다"** 라서(2차 사이클)
**그것들을 결함과 가르는 정보가 웨이퍼맵에 없을 수 있다.**

**놓친 결함의 불량률 중앙값이 0.0867 로 정상 중앙값 0.0883 보다 낮다.**
6차 그림에서 본 것과 같다 — 선이 너무 옅어 창에 안 잡힌다. **표현을 바꿔도 안 잡힌다.**

**Donut, Random, Near-full 은 한 장도 안 놓친다.**
Edge-Ring 이 놓침의 25.7% 를 차지하는 것은 **융합이 가장자리를 누른 대가**다
(die k=7 0.9692 → 융합 0.9540). 되돌리려면 헛경보가 는다 — **저울질이지 개선이 아니다.**

## 예측 채점

헛경보 다수가 진짜 불량 많음(**맞음**), 놓친 것은 볼 게 없음(**맞음**),
lot 쏠림 약함(**맞음**), **"Scratch 와 Loc 에 몰린다"(부분 오답 — Loc 은 2.4% 로 낮고
Center·Edge-Ring 이 높다)**.

## 이것이 뜻하는 것

**웨이퍼맵만 쓰는 한 큰 이득은 안 남았다.**
학습이 고칠 수 있는 상한은 놓친 결함의 49% 와 헛경보의 22% 언저리다.
**O2/O3 의 기대치를 그만큼 낮춰 잡아야 한다** — 사다리를 계속 올라가되
"학습을 넣으면 크게 오른다" 는 기대는 이 진단이 부정한다.

더 필요한 것은 **더 나은 표현이 아니라 더 많은 정보**다 — 공정 이력, 인접 웨이퍼, lot 문맥.
단 **lot 쏠림이 없으므로 단순 lot 집계는 답이 아니다.**

## 설명 없는 관측

- **Center 가 놓침률 2위(10.5%)인 이유.** 융합 AUROC 0.9412 로 높은데 95% 문턱에서
  많이 놓친다. 꼬리가 두꺼운 듯한데 분해 안 했다.
- **78% 라는 수치가 문턱에 얼마나 민감한지** 안 쟀다. 90%/99% 재현율에서도 같은지 모른다.

---

# 11차 사이클 — 봉인 홀드아웃 (2026-08-23 04:45~05:20)

## 무엇을 왜 했는가

9차 감사가 자기 한계를 정확히 적었다 — **lot 반쪽 검사는 선택 편향을 못 없앤다.
두 반쪽 다 이미 본 test 이기 때문**이다. 기획서 §11.5 가 "O2 전에 봉인 홀드아웃부터" 를
1순위로 남겼고, 이 사이클은 그것 하나만 했다. **O2 는 시작하지 않았다** — GPU 는 비어 있었지만
봉인 없이 시작하면 O2 가 같은 편향을 물려받는다.

**설계에서 가장 중요한 결정은 "봉인을 어디서 떼느냐" 였다.** 두 후보가 있었고 성질이 다르다.

1. **공식 test 에서 lot 25% 를 뗀다** — 분포는 같지만 **이미 본 웨이퍼**다.
   봉인해도 오늘의 편향은 안 없어지고, 값어치는 **O2 부터** 생긴다.
2. **공식 val 11,132장** — 놀랍게도 **OOD arm 이 한 번도 점수를 매긴 적이 없다.**
   OOD 스크립트(a21 이상, `diag_ood*`)에서 `sp["val"]` 참조가 0건이다
   (9-class 사다리 a1~a20 의 다섯 파일에만 있다).
   **지금 시점에서 편향 없는 유일한 자료**인데 분포가 다르다(유병률 0.319 대 0.067).

**둘 다 만들었다.** 하나로는 "내려갔다" 를 선택 편향과 분포 이동으로 못 가른다.
그래서 **arm 계보 여섯 개**(채택 3대 + 진짜 바닥 + 원 바닥 + 기각 arm 하나)를 같은 자로 쟀다.

크기(lot 25%), 선택 방식(라벨 비의존 md5 해시), 왜 그 크기인지(봉인 CI 반폭 0.0134 대
판정 격차 0.026~0.447, dev CI 는 0.0067 → 0.0077 로 거의 안 나빠짐)를
**실행 전에** `candidate/ood_sealed_holdout.md` 에 박았다. 판정 규칙 R1~R5 와 예측 6개도 같이.

**봉인을 코드로 강제했다**(`src/a38_sealed_holdout.py`). 문서 규약은 안 지켜진다.
`load_partition("test_sealed")` 는 실패하고, `unseal=True` **그 자체**와 `reason` 이 있어야 열리며
(`1`, `"yes"` 는 거부 — `if unseal:` 로 쓰면 실수로 열린다), 열 때마다
`unseal_log.jsonl` 에 시각과 이유가 남는다. `assert_not_sealed(idx)` 는 파티션 API 를
안 거치고 봉인 웨이퍼가 섞이는 것도 잡는다. **가드 자체에 테스트 23개**(TDD, 빨간 것 확인 후 구현).

## 결과 — 판정 규칙 넷 다 미발동

| arm | `test_dev` (편향) | `test_sealed` | `val_unseen` 유병률 맞춤 (편향 없음) |
|---|---:|---:|---:|
| **채택 fuse3** | **0.8304** | **0.8384** | **0.9291** |
| 채택 A+B (6차) | 0.8048 | 0.8104 | 0.9232 |
| 채택 A 단독 (5차) | 0.7613 | 0.7739 | 0.8113 |
| die k=7 (진짜 바닥) | 0.7043 | 0.7248 | 0.8205 |
| E0 비율 (원 바닥) | 0.3823 | 0.3939 | 0.2570 |
| 기각 선 L=7 | 0.0886 | 0.0905 | 0.0755 |

- **R1** \|봉인 − dev\| = **0.0080** (문턱 0.03) — lot 이질성 작다
- **R2** fuse3 가 **세 파티션 전부 1위**, 순위 Spearman **1.0000 / 1.0000**
- **R3** `fuse3 − die k=7` 격차 **+0.1260 / +0.1137 / +0.1086**(문턱 +0.0614) — **이득이 전이된다**
- **R4** val AUROC **0.9728**, FPR@95TPR **0.0819** — 운영 지점이 안 무너진다

**봉인이 자료를 바꾸지 않았다는 확인**: dev + 봉인을 다시 합치면 저장된 test 점수와
**최대 오차 0.0**, 합쳐 잰 blocked AUPR **0.8325**. 분할은 재계산이 아니라 재배열이다.

## 예측 채점 — 6개 중 2개 틀렸다

맞음: H1 범위, H1 Spearman, R3 전이, H2 에서도 fuse3 1위.
**틀림: "val 은 더 어려울 것"(예측 3, 4).** val AUROC 0.9728 로 test 보다 **높고**,
유병률 맞춘 AUPR 은 예측 [0.75, 0.86] 대신 **0.9291** 이었다.
**둘은 같은 뿌리다 — "분포가 다르면 어렵다" 를 근거 없이 가정했다.**

## 왜 val 이 더 쉬운가 — 자명한 바닥이 죽는 분포다

| | dev | val |
|---|---:|---:|
| 정상 불량 다이 비율 중앙값 | 0.0877 | **0.1544** |
| 결함/정상 중앙값 비 | **1.684** | **1.024** |
| E0 불량 다이 비율 AUROC | 0.7889 | **0.5777** |
| **fuse3 AUROC** | 0.9680 | **0.9728** |

**val 정상은 훨씬 더럽고, 그래서 "불량 다이를 센다" 는 축이 거의 죽는다**(비 1.02).
**그런데 창 통계는 안 무너진다.** 채택 신호가 양이 아니라 배치를 본다는 주장이
**양의 축이 죽은 분포에서 실측으로 확인됐다.**

클래스 구성 설명은 검정해서 **기각**했다 — dev 의 클래스 구성을 val 에 강제해도
fuse3 는 0.9143 으로 여전히 높다(설명되는 몫 0.015). **나머지는 설명 못 한다.**

## 이것이 뜻하는 것

1. **순위와 이득의 크기는 전이된다.** 여덟 사이클의 test 유도 반복이 사다리를 뒤집지는 않았다.
2. **그러나 수준 0.8325 는 여전히 편향된 숫자다.** H1 은 이미 본 웨이퍼라 일치가 당연하고,
   H2 는 분포가 달라 수준을 옮겨 읽을 수 없다. **오늘 증명한 것은 순위와 격차뿐이다.**
3. **자명한 바닥이 분포에 매우 약하다**(AUROC 0.79 → 0.58). 그러므로
   **절대 수치가 아니라 "바닥 대비 이득" 으로 보고해야 한다.**
4. **개발 기준선이 바뀐다.** 오늘부터 개발은 `test_dev` 에서만 한다.
   **O2 가 넘어야 할 값은 0.8325 가 아니라 `test_dev` 의 0.8304 다.**

## 규약 위반 자진 신고

**R5(각 파티션 딱 한 번)를 내가 어겼다.** 예측 3, 4 가 틀린 이유를 분해하려고
추가로 세 번 더 열었다(`test_sealed` 1회, `val_unseen` 2회).
감사 로그에 여섯 줄이 남아 있다(마지막 한 줄은 가드 실동작 점검).
추가 해제로 **arm 을 고르거나 기각하지는 않았다** — 파티션을 기술하는 데만 썼다.
그래도 **"딱 한 번" 은 지킬 수 없는 규칙이었다.**
다음 사전등록에는 **사후 기술 통계를 몇 번까지 허용하는지**를 따로 적는다.

## 설명 없는 관측

- **val 이 왜 그렇게 쉬운가.** 클래스 구성으로 설명되는 몫은 0.015 뿐이다.
  dieSize 중앙값이 845 대 533 으로 다른 것이 후보지만 **확인하지 않았다.**
- **val 에서 Scratch 만 낮다**(0.7835, n=92 대 dev 0.9197). n<100 이라 사전등록 규칙대로
  판정에 안 썼지만, 가장 자랑한 클래스가 흔들린 유일한 지점이다.
- **`test_dev` 의 FPR@95TPR 이 0.1877 로 봉인(0.1193)보다 나쁘다.** AUPR 은 반대 방향인데
  꼬리에서 갈린다. 분해 안 했다.

## 다음

1. **O2 는 `candidate/ood_o2_ssl_knn.md` 를 먼저 읽고 시작한다** — 반증 조건 7개,
   기대치(AUPR +0.01~+0.03, FPR@95 0.13 이 상한), 봉인 규약이 실행 전에 박혀 있다.
2. **개발은 `test_dev` 에서만.** `test_sealed` 는 최종 arm 확정 후 한 번.
3. val 은 소진됐다. **편향 없는 읽기는 이제 남아 있지 않다.**

---

# 12차 사이클 — Scratch 경고등 규명 (2026-08-23 15:10~15:40)

## 무엇을 왜 했는가

11차 사이클이 봉인을 열고 **경고등 하나**를 남겼다 —
`val_unseen` 에서 Scratch AUROC **0.7835 (n=92)**, dev 0.9197, 봉인 0.9414.
"n<100 이라 판정에 안 썼지만 가장 자랑한 클래스가 흔들린 유일한 지점" 이라 적혀 있었다.

**O2 를 먼저 하려 했으나 GPU 0번을 9-class 학습 큐가 계속 쓰고 있었다**
(`e22_shufflenet_v2_tr_s0` → `e22_mobilenet_v3_tr_s0`, 백본 3종 x translate x 3 seed 예정).
1~7번은 **다른 사용자(`minje`)의 작업**이라 손대지 않는다.
그래서 **CPU 로 되는 일을 먼저 했다** — 규약대로다.

**착수 전에 `candidate/ood_scratch_shift.md` 를 썼다.** 원인 후보 셋(표본 잡음 / 음성 이동 /
양성 이동), 판정 규칙 S1~S5, 예측 6개, 그리고 **11차 사이클의 R5 자진 신고에 대한 응답으로
"val 해제는 이 사이클에서 정확히 1회" 를 명시**했다. 실제로 한 번만 열었다.

## 무엇을 새로 만들었나 (TDD, 빨간 것 확인 후 구현)

- `src/a41_shape_stats.py` — 결함 형태의 기술 통계.
  굵기 대리(불량 다이의 평균 불량 8-이웃 수), 최대 8-연결성분의 이심률·주축 길이,
  동점·nan 을 처리하는 Mann-Whitney U. **어떤 채점기에도 안 들어간다**(범위 C 아님).
  테스트 18개, Mann-Whitney 는 `scipy.stats` 에 대고 박았다.
- `a21_ood_metrics.subsample_auroc_null` — **작은 n 의 AUROC 귀무분포.**
  양성만 비복원 부표집. 테스트 7개(복원 추출이면 깨지는 것 포함).
- 시험 **477 → 501개 전부 통과.**

## 결과 — 판정 규칙 S3 발동, 후보 (c)

### 2x2 교차 분해가 핵심이었다

클래스별 AUROC 은 양성과 음성 **둘 다**에 의존하는데 val 은 둘이 동시에 바뀌었다.
넷을 따로 재니 (fuse3, Scratch):

| | 음성 = dev 정상 | 음성 = val 정상 |
|---|---:|---:|
| 양성 = dev Scratch | **0.9197** | 0.8434 |
| 양성 = val Scratch | **0.8521** | 0.7835 |

음성 교체 -0.076, 양성 교체 -0.068, 거의 가법적.

**그리고 Scratch 는 val 양성이 더 어려운 유일한 클래스다** —
Center 0.9377→0.9981, Edge-Loc 0.9798→0.9970, Edge-Ring 0.9533→0.9754,
Loc 0.9828→0.9902 는 전부 **쉬워진다.**

### 표본 잡음이 아니다

dev Scratch 517장에서 92장을 2,000번 비복원 부표집한 귀무는 **[0.8928, 0.9429]**,
관측 0.8521 은 **2,000회 중 한 번도 안 나왔다**(분위 0.0000).

### 기작은 크기가 아니라 굵기와 이심률이다

형태 통계(Bonferroni 0.0083):

| | dev Scratch | val Scratch | p |
|---|---:|---:|---|
| die_size | 904 | **2,393** | 4.7e-30 |
| **굵기 대리** | **1.3253** | **1.1380** | 2.7e-03 |
| **이심률** | **4.54** | **6.29** | 2.1e-10 |
| 주축 길이 | 13.8 | 18.8 | 2.9e-07 |
| 불량 비율 | 0.1028 | 0.0509 | 1.0e-11 |
| 최대 연결성분 | 18 | 20 | 9.6e-02 (**차이 못 봄**) |

**대조로 정상끼리도 쟀더니 두 핵심 축이 반대 방향이었다.**
이심률: 정상 dev 2.28 → val 2.18(**낮아짐**), Scratch 4.54 → 6.29(**높아짐**).
굵기: 정상 0.765 → 1.132(**두꺼워짐**), Scratch 1.33 → 1.14(**가늘어짐**).
**val 에서 정상은 뭉치고 긁힘은 가늘어져 둘이 서로 가까워진다.**

### 왜 그것이 점수를 깎는가 — dev 안에서 확인 (탐색)

`src/diag_ood_scratch_size_dev.py`. **val 을 다시 열지 않았다.**
dev Scratch 안에서 이상 점수 순위와 형태의 Spearman:

| arm | die_size | **굵기** | **이심률** | 최대성분 |
|---|---:|---:|---:|---:|
| fuse3 | **0.011** | **0.795** | **-0.122** | 0.803 |
| A | -0.058 | 0.810 | **-0.343** | 0.791 |
| C | -0.020 | 0.758 | **-0.296** | 0.715 |
| **B 선 L=11** | 0.045 | 0.493 | **+0.127** | 0.547 |

**가늘고 긴 것을 좋아하는 요소는 선 필터 하나뿐이고 A, C 는 적극적으로 싫어한다.**
동일 가중 융합에서 **2대 1로 진다.** val Scratch 는 정확히 그 방향으로 이동해 있다.

**크기는 원인이 아니다** — dev 안에서 Spearman 0.011, 크기 구간별 Scratch AUROC 은
0.9197 / 0.9402 / 0.9409 / 0.9332 / 0.9287 로 **1600 까지 평평하다.**

## 정정 13 — "표본 부족" 을 "약점 아님" 으로 바꿔 적었다

크기 맞춘 귀무를 만들려다 **실패**했다. val 크기 구성을 dev 에 강제하려면
`>1600` 에서 66장이 필요한데 **dev Scratch 에는 6장뿐**이다.

| | `>1600` Scratch |
|---|---|
| dev | **6 / 517 (1.2%)** |
| val | **66 / 92 (71.7%)** |

`docs/reports/OOD.md` 는 `>1600` 을 이렇게 적었다:
**"낮은 수치는 약점이 아니라 표본 부족이다 ... 원인을 찾을 만한 신호가 애초에 없다."**

**앞 절은 맞고 뒤 절이 틀렸다.** 표본이 없으면 나오는 결론은 "약점이 아니다" 가 아니라
**"판정할 수 없다"** 다. 그리고 val 은 그 구간이 71.7% 인 분포이며 거기서 Scratch 가 약하다.
(참고로 dev 의 `>1600` Scratch 6장은 fuse3 AUROC **0.3255** 로 무작위보다 낮다.
**n=6 이라 판정에 쓰지 않는다** — 그 점을 여기 적어 두는 이유는 이번에도 같은 실수,
즉 작은 n 에서 결론을 뽑는 실수를 하지 않기 위해서다.)

## 예측 채점 — 6개 중 2개 틀렸다

맞음: 2(방향), 3(B 가 가장 많이 떨어짐), 4(크기가 크다), 5(불량 비율 낮다).
**틀림: 1(표본 잡음으로 설명된다), 6(굵기는 차이 안 보인다).**
**둘은 같은 뿌리다 — 나는 이 경고등이 잡음이길 기대했고, 그래서 기작이 없을 것이라 예측했다.**

## 무엇을 하지 않았는가

**선 필터를 고치지 않았다. 융합 가중을 바꾸지 않았다. 채택 표를 안 건드렸다.**
val 에서 나온 관측으로 arm 을 고치면 val 에 적합시키는 것이다(사전등록 §6-4).

## 다음

**요소 가중이 값어치 있는 다음 arm이다.** 현재 동일 가중 Fisher 인데,
가는 긁힘에서 B 만 옳고 A, C 가 틀리는데 셋을 똑같이 센다.
**train-none 만으로 가중을 정하는 방법**(요소 간 상관을 정상에서 재서 백색화)은
범위 A 안에 있고 test 로 고르지 않는다. **dev 에서 사전등록해 잰다.**

---

# 13차 사이클 — 3단계 MLLM 간접평가 사전등록 (2026-08-23 15:40~16:0x)

## 무엇을 왜 했는가

GPU 0번이 계속 9-class 큐에 잡혀 있어 O2 를 못 시작한다.
**할 일이 떨어지면 급한 실험을 돌리지 말고 사전등록 문서를 쓴다** 는 규약대로,
3단계(MLLM 8종 분류)의 **간접 평가 설계**를 실행 전에 박았다.
`docs/experiments/candidate/ood_stage3_map_handoff.md`.

**MLLM 을 한 번도 호출하지 않았다.** 이 워크스트림에 접근 경로가 없고 웹 검색도 소진됐다.
**이 문서는 설계이고 판정이 아니다.** 그 사실을 §13 에 적었다.

## 실행 전에 못 박은 것

1. **맵은 정보를 더하지 않는다.** 세 요소 맵은 원본의 **결정론적 함수**다.
   그러므로 질문은 "정보가 느는가" 가 아니라
   **"이미 다 들어 있는 정보를 미리 계산해 보여 주는 것이 읽는 쪽에 도움이 되는가"** 다.
   이걸 실행 전에 안 적으면 결과를 잘못 읽게 된다.
2. **arm 넷** — C0 원본만 / E1 원본+그 웨이퍼의 맵+스칼라 /
   **P1 위약(다른 웨이퍼의 맵, 이미지 수와 토큰 수 동일)** / S1 원본+스칼라만.
   - **E1 − P1 = 맵 내용의 기여.** 이 대조가 없으면 "그림을 더 주면 그냥 잘한다" 를 못 뺀다.
   - **E1 − S1 = 맵 이미지가 스칼라 위에 더하는 것.**
   - **S1 − C0 = 스칼라만의 기여.** 8차 감사가 이게 클 것이라 예고했다(28쌍 중 18에서 1위).
3. **렌더링 규약** — 척도는 train-none 1/99 백분위로 고정(test 통계를 안 쓴다),
   최근접 이웃 8배 확대(보간 금지, 명목형 격자), 이미지 순서 고정.
4. **평가 집합** — 맵 npz 12,894장 중 **9,509장이 `test_dev`, 3,385장이 봉인**(실측).
   봉인은 안 만진다. 주 집합은 **클래스당 70장 균형 560장**(최소 클래스 Near-full 70).
5. **검정력을 실행 전에 적었다** — macro-F1 차이 **0.05 이상은 가리고 0.02 이하는 못 가린다.**
   무승부일 때 "검정력이 모자랐다" 를 사후에 꺼내 쓰지 않기 위해서다.
6. **오염을 정직하게 적었다** — WM-811K 는 공개 데이터셋이고 **MLLM 이 이미 봤을 수 있다.**
   zero-shot 점수를 "결함 라벨을 안 썼다" 의 증거로 쓰지 않는다.
   **다만 네 arm 이 같은 오염을 공유하므로 arm 간 차이는 유효하다** — 대조군을 넷으로 만든
   또 하나의 이유다.

## 반증 조건 7개, 예측 5개

핵심 예측: **E1 − S1 의 CI 가 0 을 포함한다**(맵 이미지는 스칼라 위에 못 보탠다).
맞으면 3단계에 넘길 것은 맵이 아니라 **스칼라 두 개**이고 맵은 사람이 보는 설명으로만 남는다.
**파이프라인이 훨씬 싸지므로 그것이 가장 값어치 있는 결과일 수 있다.**

---

# 14차 사이클 — 요소 가중, Fisher 의 독립 가정 (2026-08-23 16:10~16:22)

## 무엇을 왜 했는가

12차 사이클이 만든 다음 arm 이다. 가는 긁힘에서 **선 필터 B 만 옳고 A, C 는 틀리는데**
동일 가중 Fisher 는 셋을 똑같이 센다(이심률 상관 A -0.343, C -0.296, **B +0.127**).

**그런데 이 arm 의 논거는 val 없이도 선다.** Fisher 의 결합 통계량은 p 값이
**독립일 때만** 그 분포를 갖는데, 요소 A(k2 5x5+반경보정)와 C(k2 3x3)는
**둘 다 k2 밀도의 최대값**이라 구조적으로 독립일 수 없다.
가중을 **train-none 공분산**에서 유도하면(`w ∝ S^-1 1`) test 로 고를 자유도가 0 이다.

**출처를 사전등록에 정직하게 적었다** — 이 가설을 떠올린 계기는 val 관측이다.
val 을 방법 선택에 쓰지 말라는 규약이 있으므로 숨기지 않았다.
**arm 자체는 val 을 참조하지 않고 정의되고, 평가는 `test_dev` 에서만 했다.**

**W0 대조군**(동일 가중 probit 합)을 같이 뒀다 — 이득이 "가중" 때문인지
"Fisher→probit 변환" 때문인지 못 가르면 판정이 무효이기 때문이다.

## 결과 — 기각. 그런데 기각의 분해가 산출물이다

train-none 상관: **A-C 0.800**, A-B 0.758, B-C 0.677. **어느 쌍도 독립이 아니다.**
유도된 가중: A +0.3652, B +0.3660, C **+0.2688**(동일 가중이면 0.3333).

| arm | AUPR블록 | FPR@95 |
|---|---:|---:|
| **채택 fuse3 (동일 가중 Fisher)** | **0.8304** | **0.1877** |
| W0 대조 (동일 가중 probit 합) | 0.8187 | 0.2071 |
| W1 주 (S^-1 가중 probit 합) | 0.8174 | 0.2285 |

- W1 vs fuse3 **[-0.0145, -0.0116]** p=0.0000 → **반증 조건 1 발동, 기각**
- W0 vs fuse3 **[-0.0130, -0.0106]**
- W1 vs W0 **[-0.0019, -0.0007]**

**손해 -0.0130 중 -0.0118 이 probit 변환에서 오고 가중은 -0.0012 뿐이다.**
**진짜로 일하던 것은 요소 가중이 아니라 Fisher 의 꼬리 강조였다.**
실행 전 예측 5 에 "차이가 크면 Fisher 의 꼬리 강조가 실제로 일하고 있었다는 뜻이고
예상 밖이므로 그렇게 보고한다" 를 박아 뒀고 **그 조항을 그대로 적용했다.**

## 가중은 예측한 방향으로 정확히 움직였다 — 그런데 총합으로는 손해다

W1 대 W0(변환 고정, 가중만 변경)의 클래스별 차이:
**Scratch +0.0042(1위), Edge-Ring +0.0014, Loc -0.0017, Center -0.0095.**

**12차가 예고한 방향 그대로다.** 그런데 Center 가 두 배 이상 내려간다.
이유: **C 는 A 와 가장 닮았지만(0.800) 동시에 Center 를 가장 잘 보는 요소다**
(단독 Center: C 0.9588 > A 0.8710 > B 0.8118).
`1' S^-1 1` 은 "셋이 같은 공통 이동을 잰다" 고 가정하는데
**이 요소들은 서로 다른 결함을 잰다.** 기작 이해는 맞았고 **처방이 틀렸다.**

## 예측 채점 — 5개 중 2개 틀렸다

맞음: 2(Scratch 가 가장 오름), 3(A-C 상관 최고), 4(형식상 B 가중 최대).
**틀림: 1(이길 것), 5(W0 는 fuse3 와 거의 같을 것).**

---

# 15차 사이클 — 결합 규칙의 꼬리 강조 (2026-08-23 16:25~16:33)

## 무엇을 왜 했는가

14차가 만든 질문이다. **"더 세게 강조하면 더 좋은가."**

"얼마나 세게" 를 연속 파라미터로 두면 그건 test 튜닝이다. 그래서
**파라미터가 0개인 고전 결합 규칙 다섯**만 쓰고, 꼬리 강조가 약한 순서로
**실행 전에 배열해** 두었다. 요소도 가중도 안 바꾸고 **결합 규칙 하나만** 바꾼다.

사전등록에 프레이밍을 박았다 — **이기는 규칙을 찾는 것이 아니라
14차의 설명이 옳은지 재는 것**이 목적이다. 어느 쪽이 나와도 배운다.

## 결과 — H(단조)는 반증됐다. 그런데 다섯을 둘로 갈라야 한다

| # | 규칙 | AUROC | **AUPR블록** | FPR@95 | **고유값** |
|---:|---|---:|---:|---:|---:|
| 1 | Wilkinson 최소 (연접) | 0.9425 | 0.7369 | 0.3095 | 263 |
| 2 | Edgington 합 | 0.9565 | 0.7800 | 0.2584 | 3,556 |
| 3 | Stouffer probit 합 | 0.9646 | 0.8187 | 0.2071 | 3,647 |
| **4** | **Fisher (채택)** | **0.9680** | **0.8304** | 0.1877 | 3,647 |
| 5 | Tippett 최대 (이접) | 0.9679 | 0.7801 | **0.1742** | **204** |

전부 Fisher 에 짝지어 진다(p=0.0000). **주 지표가 4에서 봉우리다 → H 반증.**

**그런데 합 규칙 셋(2,3,4)만 보면 꼬리 강조에 완전히 단조다**
(0.7800 → 0.8187 → 0.8304). 같은 요소, 같은 동일 가중, 같은 눈금(3,556/3,647/3,647)인데
**변환의 꼬리 모양만으로** 갈린다. **14차의 설명이 재현되고 확장됐다.**

**최소·최대는 그 축 위에 있지 않다** — 꼬리 강조의 극단이 아니라 **결합을 그만두는 것**이다.

## Tippett 이 진 이유는 꼬리가 아니라 눈금이다

| 규칙 | 고유값 | **95% 재현율 문턱 동점 웨이퍼** |
|---|---:|---:|
| Fisher | 3,647 | **23** |
| Tippett 최대 | **204** | **8,776** |

요소 B 는 고유값 **11개**, C 는 **9개**다. 최대를 취하면 그 거친 눈금이 그대로 남고
합을 취하면 셋이 섞여 3,647개로 쪼개진다.
**지표의 트집이 아니라 실제 운영 결함이다** — 95% 재현율 지점에서 8,776장이 같은 점수면
팹은 그 지점을 **고를 수 없다.** 이 워크스트림이 세운 "고유값을 함께 읽어라" 가
**이번엔 arm 을 기각시켰다.**

## 숨기지 않을 것 — Tippett 은 세 곳에서 채택 arm 을 이긴다

FPR@95TPR **0.1742**(Fisher 0.1877), 95% 재현율 헛경보 **14,269**(15,370),
**Center 0.9525 [0.9430,0.9614]** 대 0.9377 [0.9269,0.9490] — **CI 가 안 겹친다.**
Scratch 0.9249 대 0.9197(12차가 예고한 방향, 예측 5 적중).
대신 80% 재현율 헛경보는 3,180 대 2,173 으로 훨씬 나쁘다.

**하나의 결합 규칙이 모든 결함에 최적일 수 없다.** 12차가 Scratch 에서 본 구조와 같다.

## 예측 채점 — 5개 중 1개 틀렸다

맞음: 1, 2, 3, 5. **틀림: 4(Tippett > Stouffer)** — AUROC 로는 맞는데(0.9679 대 0.9646)
주 지표로는 틀렸다. **이유가 눈금이라는 것이 이 사이클의 핵심이다.**

## 판정

**다섯 중 아무것도 채택하지 않는다. fuse3 유지.**
반증 조건 1(Tippett 이 Fisher 를 못 넘음), 3(고유값 1,000 미만), 4(크기 구멍에서 하락)이
발동했고, 조건 5("결과 보고 고르지 않는다")를 지켰다.

## 모르는 것

- **Fisher 가 봉우리인지, 내가 시험한 파라미터 없는 규칙 중 꼬리가 가장 센 것일 뿐인지 모른다.**
  더 센 합 규칙에는 연속 파라미터가 필요하고 그것을 test 로 고르면 튜닝이다.
  **정상 데이터만으로 꼬리 강도를 고르는 방법은 여전히 모른다.**
- 요소 B, C 의 눈금을 잘게 만들면 최대 규칙이 살아나는지 **안 재봤다.**

---

# 16차 사이클 — 3단계 평가 자료를 실제로 지었다 (2026-08-23 17:00~17:10)

## 무엇을 왜 했는가

13차에서 3단계(MLLM 8종 분류) 간접 평가를 사전등록했고, 그때 "아직 안 한 것" 으로
**렌더링 코드, 프롬프트 문자열, 응답 파싱 규약** 셋을 꼽았다. **셋 다 했다.**

**MLLM 은 여전히 한 번도 호출하지 않았다. 판정은 없다.**
O2 가 CPU 를 쓰는 동안 **문서가 이미 정한 규약을 코드로 옮기는 일**만 했다 —
새 자유도를 만들지 않았다.

## 새 모듈 (TDD, 빨간 것 확인 후 구현)

- `src/a48_stage3_render.py` — 렌더링(§5), arm 별 프롬프트(§4), 응답 파싱(§7). **시험 21개**
- `src/a49_stage3_build.py` — 자료 생성. 봉인 가드 포함
- `result/ood/o3_stage3/` — 이미지 2,240장, arm 넷 프롬프트 각 560개, manifest (13 MB)

시험에서 박은 것은 문서가 정한 조항 그대로다.
**웨이퍼 밖은 순수 검정**, **최근접 이웃 확대**(보간하면 명목형 격자가 뭉개진다),
**원본은 3색뿐**, **척도는 train-none 에서**, **이미지 순서 고정**,
**위약은 다른 웨이퍼의 맵과 스칼라**, **거부와 형식 위반은 오답**.

## 실측으로 확인한 것

- **560장, 클래스당 정확히 70장.** 전부 `test_dev` 안이고 `assert_not_sealed` 통과.
  **봉인 3,385장은 안 만졌다.**
- **척도는 train-none 29,149장의 맵에서** 잡았다:
  A [0.12429, 0.99133], B [0.00000, 0.45455], C [0.00000, 0.44444].
- **원본 렌더링의 색이 정확히 3개다.** 512x512 가 64x64 의 정확한 8배 블록이다.
- **위약 짝의 고정점 0개.** 자기 자신과 짝지어지면 실험군과 같아지는데 그런 경우가 없다.

## 응답을 보기 전에 예측 하나를 더 박았다

**요소 B, C 의 척도 상한이 0.4545, 0.4444 로 낮다** — 두 맵은 대부분 검고 몇 칸만 밝다.
**MLLM 이 거기서 읽을 것이 별로 없을 것**이고, 이는 13차 예측 2
("맵 이미지는 스칼라 위에 못 보탠다")를 강화하는 방향이다.
**자료를 짓고 나서, 응답을 보기 전에 적었다.**

---

# 17차 사이클 — O2 자기지도 인코더 + kNN (2026-08-23 16:04~17:45, CPU 병행)

## 무엇을 왜 했는가

이 워크스트림의 1순위였다. 사전등록 `candidate/ood_o2_ssl_knn.md` 에 반증 조건 7개와
기대치가 **어젯밤에 이미** 박혀 있었고, 그대로 따랐다.

**GPU 0번은 하루 종일 9-class 학습 큐가 썼다**(shufflenet_v2, mobilenet_v3 x translate x 3 seed).
1~7번은 **다른 사용자의 작업**이라 손대지 않는다. 그래서 **CPU 로 끝나게 규모를 다시 잡았고**,
그 설계를 `candidate/ood_o2_ssl_knn.md` §10 에 **코드보다 먼저** 박았다.

## 설계에서 실행 전에 정한 것 (§10)

- **수용영역을 정확히 7x7 로 고정**(3x3 conv 3층). 전역 수용영역으로 재면
  이득이 **학습**에서 온 것인지 **창을 키워서** 온 것인지 못 가른다.
  채택 신호가 3x3, 5x5 창이고 진짜 바닥이 국소 밀도 7x7 이므로 같은 창에서 겨루게 했다.
- **7 을 고른 근거는 실측이다**: 온전 창이 하나도 없는 train-none 웨이퍼가
  11x11 에서 **7.07%**, 9x9 에서 1.24%, **7x7 에서 0.11%**.
- **한계를 미리 적었다**: 요소 B(선 L=11)는 이 수용영역 밖이다.
  **그래야 나중에 그 설명을 사후에 꺼내지 않는다.**
- **온전 창만 점수를 낸다**(정정 11). 뱅크도 질의도 학습 손실도 전부.
- **무작위 초기화 대조군을 반드시 같이 돌린다**(§10.5). 없으면 판정 무효다.
- 시험 17개(TDD). 수용영역이 정확히 7 이라는 것을 **기울기 지지집합**으로 박았다.

## 결과 — 기각

| seed | 무작위 초기화 | 자기지도 학습 후 | 짝지은 차이 |
|---:|---:|---:|---|
| 0 | 0.4986 | **0.3283** | [-0.1801, -0.1589] **해로움** |
| 1 | 0.4692 | **0.5524** | [+0.0717, +0.0936] **도움** |
| 2 | 0.3575 | **0.3307** | [-0.0416, -0.0152] **해로움** |

**부호가 갈린다.** 두 arm 다 seed 폭이 크다(무작위 0.141, 학습 0.224).
fuse3(0.8304) 대비는 세 seed 다 [-0.51, -0.27] 로 **크게 진다.**
반증 조건 1, 2, 3, 6, 8 이 발동했다. **채택 arm 이 없으므로 봉인은 열지 않았다.**

융합 arm(O2 ⊕ fuse3)은 0.8210 / 0.8292 / 0.8296 — **한 번 지고 두 번 무승부.**
**O2 는 fuse3 에 아무것도 못 보탠다.**

## 이 사이클에서 내가 두 번 틀렸다

### (A) seed 0 하나로 결론을 내고 기작까지 붙였다

17:00 경 **"자기지도 학습이 표현을 악화시켰다"** 고 중간 보고하면서
유효차원 14.67 → 1.23 이라는 기작까지 붙였다. **seed 1 이 부호를 뒤집었다.**

**기작 쪽이 더 나쁘다.** seed 1 에서 붕괴 진단을 다시 돌렸더니
**붕괴는 똑같이 일어나는데**(13.49 → 1.41, 결함→최근접정상 0.03033 → 0.00078)
**성능은 반대로 갔다**(0.4692 → 0.5524).
**"붕괴 때문에 나빠졌다" 는 인과 주장이 틀렸다.**

살아남는 주장은 이것뿐이고 두 seed 에서 재현된다 —
**masked die prediction 은 32차원 표현을 사실상 1차원으로 무너뜨린다**(유효차원 1.2~1.4).
**그 하나 남은 방향의 품질은 초기화가 정하고, 어느 쪽이든 창 통계 근처에 못 온다.**

### (B) 사전등록의 변동 폭 가정이 4배 틀렸다

§5-2 에 "레포 실측 run-to-run 범위는 **0.050**" 이라 적었는데 실측은 **0.224 / 0.141** 이다.
0.050 은 **9-class 분류 macro-F1** 의 변동에서 가져온 숫자다 —
**다른 과제, 다른 지표, 다른 모형의 변동 폭을 이 arm 에 옮겨 썼다.** 정정 12 와 같은 실수다.
판정은 안 바뀌었지만(이미 3 seed 를 요구했다), **이득이 0.05 근처였다면 못 갈랐을 것**이다.

## 무엇이 판정을 살렸나

**무작위 초기화 대조군이다.** 그것이 없었으면 "0.33 이니 학습이 약하다" 로 끝났고,
"학습이 무작위 초기화보다도 나쁠 수 있다" 도 "seed 에 따라 반대로 간다" 도 못 봤다.

## 다음

**사전등록해 둔 학습량 곡선**(`candidate/ood_o2_training_amount.md`)을
**착수 직전에 3 seed 로 개정**했다(§6.1). 1 seed 로는 이 곡선을 못 읽는다.
격자 `{0,1,3,6,12}` 는 원안 그대로 두고 seed 만 늘렸다 — **더 엄격한 쪽이다.**

---

# 18차 사이클 — 큰 웨이퍼의 헛경보 (2026-08-23 18:20~18:40)

## 무엇을 왜 했는가

정정 13 이 남긴 구멍이다. `>1600` 구간을 **"약점이 아니라 표본 부족"** 이라 적었던 것을
12차에서 **"판정할 수 없다" 가 맞다**고 정정했는데, 거기서 멈춰 있었다.

**그런데 절반은 잴 수 있다.** 재현율은 결함이 있어야 재지만
**헛경보는 정상만 있으면 잰다** — `test_dev` 에 그 구간 정상이 446장 있다.
라벨은 **문턱을 정할 때만** 쓰고 구간별 초과 비율에는 안 들어간다.

**먼저 사실관계를 바로잡았다**: `>1600` 은 train-none 이 2,865장으로 오히려 넉넉하다.
모자란 것은 **평가할 결함**이다(전체 test 에 33장). **적합 구멍이 아니라 평가 구멍이다.**

## 결과 — 예측 1 이 틀렸다

| 구간 | dev 정상 | 재현율 50% | 재현율 95% | 95% Wilson CI |
|---|---:|---:|---:|---|
| 562-776 | 17,280 | 0.0004 | 0.1051 | [0.1006, 0.1098] |
| 776-1090 | 20,652 | 0.0013 | 0.2021 | [0.1966, 0.2076] |
| 1090-1334 | 15,898 | 0.0019 | 0.1629 | [0.1573, 0.1687] |
| 1334-1600 | 16,307 | 0.0044 | 0.1446 | [0.1393, 0.1501] |
| **>1600** | **446** | **0.0336** | **0.3991** | **[0.3547, 0.4452]** |

**`>1600` 이 모든 문턱에서 가장 나쁘다.** 재현율 50% 에서 다른 구간의 **8~84배**,
95% 에서 네 구간과 **CI 가 안 겹친다.**

나는 "채택 신호가 전부 크기 불변이니 헛경보율도 같을 것" 이라 예측했다.
**논거는 맞고 결론이 틀렸다** — 크기 불변인 신호가 크기마다 다른 헛경보율을 낸다는 것은
**큰 웨이퍼의 정상이 실제로 다르게 생겼다**는 뜻이다. **채점기 결함이 아니라 모집단 성질이다.**

## 예상 밖 — 희소 구간에서 train-none 이 dev 정상을 대표하지 못한다

같은 95% 문턱에서 **`1090-1334` 의 train-none 218장은 89%가 문턱을 넘는데
같은 구간 dev 정상 15,898장은 16%다.** 예측 3 과 정반대다.

보정이 **train-none 전체를 한 덩어리로** 적합하므로 장수가 많은 구간(400-562 의 22,530장)이
지배하고 희소 구간이 그 지배를 받는다.
보고서는 그 두 구간을 "진짜 커버리지 구멍" 이라 적으면서 **"그런데 거기서 안 무너진다"**
(AUPR 0.7997, 0.7905)고 했다. AUPR 로는 맞다. 그러나
**커버리지 구멍이 아무 흔적도 안 남긴다는 인상은 틀렸다.**

## 못 잰 절반은 그대로다

`>1600` 재현율은 dev 결함 **29장**으로 판정할 수 없다. val 은 소진됐고
**봉인은 표본 보충용이 아니라 안 연다.** 이제 정정 13 을 더 정확히 쓸 수 있다 —
**낮은 AUPR 은 적어도 부분적으로 헛경보 탓이고 그건 실측됐다. 놓침 몫은 판정 불가다.**

## 무엇을 하지 않았는가

**채점기를 안 고쳤다.** 구간별 보정(대역을 크기로도 나누기)이 자연스러운 처방이지만
그건 새 arm 이고 **별도 사전등록이 필요하다.**

## 부수 — 다음 세션용 사전등록 둘을 미리 썼다

- `candidate/ood_tail_strength_selection.md` — 15차가 남긴 "꼬리 강도를 정상만으로
  고를 수 있는가". **주장은 "못 한다" 이고 그 이유가 구체적이다** —
  꼬리 강도는 귀무가 아니라 **대립가설에 대한 진술**이라 정상 데이터에 정보가 없다.
  A-갈래(운영 가능성)와 B-갈래(합성 대립가설)를 갈라 놨다.
- `candidate/ood_large_wafer_coverage.md` — 이 사이클이 그 문서의 판정 절이다.

---

# 19차 사이클 — 깊이별 붕괴, 내 기작 주장의 검정 (2026-08-23 18:08~20:20)

## 무엇을 왜 했는가

17차에서 **"머리가 요구하는 출력이 스칼라 하나라 마지막 층이 그쪽으로 붕괴한다"** 고 적었다.
**그건 설명이지 측정이 아니다.** 정정 6, 11, 14 가 전부 같은 실수였으므로 검정했다.

**주장이 참이면 머리에서 멀수록 덜 무너져야 한다.**
깊이 1(머리에서 2층)과 깊이 3(머리 바로 앞)을 **무작위 초기화 짝과 함께** 잰다.

**교란을 실행 전에 인정했다**: 깊이를 바꾸면 수용영역도 채널 수도 같이 바뀐다.
그래서 **판정을 성능이 아니라 "붕괴 비율"(학습 유효차원 / 무작위 유효차원)로** 하기로
사전등록에 박았다. **그 결정이 판정을 살렸다**(아래 예측 2).

## 결과 — 반증 조건 미발동

| seed | 깊이 1 (RF 3x3, 24채널) | 깊이 3 (RF 7x7, 32채널) |
|---:|---:|---:|
| 0 | **0.593** | **0.084** |
| 1 | **0.576** | **0.112** |
| 2 | **0.499** | **0.105** |

**세 seed 전부 머리에 가까울수록 심하게 무너진다.** 기작 주장은 반증되지 않았다.
**다만 교란 때문에 확증도 아니다.**

## 가장 값어치 있는 줄 — 수용영역 교란이 없는 비교

깊이 1 은 **수용영역이 정확히 3x3** 이고 이는 **요소 C 와 같은 창**이다.

| | AUPR블록 |
|---|---:|
| **요소 C — 3x3 창 안 불량 다이를 세기만 한다** | **0.6817** |
| 학습된 24채널 필터뱅크, 같은 3x3 창 | **0.2681 / 0.2374 / 0.1448** |

17차 결론에는 "요소 B(선 L=11)가 수용영역 밖" 이라는 변명거리가 남아 있었다.
**여기엔 없다. 같은 창에서 학습된 표현이 자명한 셈에 크게 진다.**

## 예측 채점 — 5개 중 3개 틀렸다

맞음: 1(유효차원 1층>3층), 4(fuse3 못 넘음), **5(요소 C 를 못 넘음)**.
**틀림: 2(무작위 초기화의 깊이별 차이가 작을 것)** — 6.10~7.17 대 11.89~14.67 로 2배다.
**틀림: 3(1층이 3층보다 나을 것)** — 세 seed 전부 반대다.

**예측 2 가 틀린 것이 방법론적으로 중요하다.** 깊이만 바꿔도 유효차원이 2배 달라지므로
**"학습 후 1층 차원이 높다" 만으로는 아무것도 못 말한다.**
무작위 초기화 짝을 실행 전에 박아 두지 않았으면 그 오답이 결론을 오염시켰을 것이다.

**예측 3 이 틀린 것도 뜻이 있다 — 덜 무너진 층이 더 나쁘다.**
정정 14 에서 기각한 "붕괴가 성능을 결정한다" 가 **또 부정됐다.**

## 부수 발견 — "결정론적" 에는 스레드 수가 붙어야 한다

3층 arm 이 17차 값을 재현해야 하는데 seed 0 에서 0.3304 대 0.3283 으로 어긋났다.
원인을 직접 쟀다.

- **순전파는 4 / 18 / 28 스레드에서 비트 단위로 같다**(최대 절대차 0.0)
- **학습은 다르다** — 같은 시드 1 epoch 에 가중치가 **최대 4.3e-3** 갈린다

그래서 **무작위 초기화 arm 은 스레드 수와 무관하게 완전히 재현되고
학습 arm 만 3자리에서 갈린다**(최대 0.0078). 결론이 서 있는 격차(0.084 대 0.59)보다
두 자릿수 작으므로 **재현된 것으로 판정**했다. **재현 명령에 `--threads` 를 같이 적는다.**

## 이 사이클이 잡은 버그 둘

- **특징 차원을 32 로 하드코딩한 곳 셋**(`score_partition`, `patch_feats`, `gather_bank`).
  깊이 1(24채널)에서 터졌다. **두 곳만 고치고 돌렸다가 세 번째에서 다시 터졌다** —
  그래서 시험을 **세 진입점 전부**에 걸었다.
- **O2 사전등록 §5-5 가 요구한 one-class 비트 시험을 17차에서 안 썼다.**
  진입점 라벨 검사만 하고 "라벨 가드 통과" 라 적었는데 **그건 픽셀이 안 샜다는 증명이 아니다.**
  `a43` 에 `select_train_none` 과 `o2_scores` 를 빼내 파이프라인 전체를 시험 가능하게 하고
  **train 결함 픽셀을 망가뜨려도 test 점수가 비트 단위로 같은 것**을 박았다. 성질은 성립한다.
  **시험이 뒤늦었다는 사실 자체를 기록한다.**

---

# 20차 사이클 — 학습량 곡선, 3 seed (2026-08-23 17:49~20:55)

## 무엇을 왜 했는가

17차가 "학습이 해로운가" 를 못 갈랐다(seed 부호가 갈렸다). 두 가설이 있었다 —
**(a) 과학습**(적게 하면 도움) 대 **(b) 과제 부적합**(학습량과 무관).

**착수 직전에 사전등록을 1 seed → 3 seed 로 개정했다**(§6.1, 원안을 안 고치고 별도 절).
근거: O2 실측 seed 폭이 0.224 / 0.141 로 **사전등록이 가정한 0.050 의 4배**였다.
격자 `{0,1,3,6,12}` 는 원안 그대로 뒀다 — **결과 보고 격자를 바꾸지 않는다.**

## 결과 — 가설 (b) 기각. 과학습이 맞다

| epoch | seed 0 | seed 1 | seed 2 | 평균 | 짝지은 차이 (ep0 대비) |
|---:|---:|---:|---:|---:|---|
| 0 | 0.4986 | 0.4692 | 0.3575 | 0.4418 | — |
| **1** | 0.5508 | 0.5527 | 0.6236 | 0.5757 | **+0.052 / +0.084 / +0.266** |
| **3** | **0.6202** | **0.6474** | **0.6450** | **0.6375** | **+0.122 / +0.178 / +0.288** |
| 6 | 0.6045 | 0.5636 | 0.2135 | 0.4605 | 부호 갈림 |
| 12 | 0.3283 | 0.5524 | 0.3307 | 0.4038 | 부호 갈림 |

**세 seed 가 전부 `epoch=3` 에서 최대다.** `epoch=1` 과 `3` 에서 짝지은 차이가
**세 seed 다 양수**이므로 개정된 반증 조건 4 가 발동해 **가설 (b)는 기각**이다.

`epoch=0` 과 `epoch=12` 는 17차 O2 의 값을 **세 seed 다 재현**한다.

## 정정 16 — 사전등록이 손잡이 하나를 근거 없이 박았다

O2 §10.3 은 "Adam lr 1e-3, cosine, **12 epoch**, batch 256" 이라 적었고
**왜 12 인지 아무 근거도 대지 않았다.** 그게 과학습 지점이었다.

그래서 17차에서 **"학습이 무작위 초기화보다 나쁠 수 있다"** 는 인상이 생겼는데,
**`epoch=3` 에서는 무작위 초기화 대비 평균 +0.196 이고 세 seed 다 양수다.**
**자기지도 학습은 이 표현에 도움이 된다.**

**O2 의 기각 자체는 유지된다** — `epoch=3` 의 최고 0.6474 도 fuse3 0.8304 에 **-0.183** 이다.
**바뀌는 것은 결론이 아니라 결론의 이유다.**

## 예측 5 가 이 사이클의 핵심 — 이제 측정됐다

masked CE 는 세 seed 다 **단조로 준다**(seed 2: 0.6211 → 0.3741 → 0.3207 → 0.2929).
같은 구간 blocked AUPR 은 **0.6236 → 0.6450 → 0.2135 → 0.3307**.

> **pretext 과제를 더 잘 푸는 것과 이상탐지에 좋은 표현을 갖는 것은 다른 것이다.**

옆 워크스트림에서 같은 과제가 9-class 사전학습에 해로웠던 것과 방향이 같고,
**여기서는 갈림이 언제 일어나는지까지 보인다 — `epoch=3` 이후다.**

## 유효 차원은 성능을 결정하지 않는다 — 네 번째 확인

**seed 1 은 유효차원 1.28(ep3)에서 최고이고 1.51(ep12)에서 최저다.**
차원이 더 높은 쪽이 더 나쁘다. 정정 14 가 또 확인됐다.

## 예측 채점 — 5개 중 2개 틀렸다

맞음: 2(차원 감소), 4(fuse3 못 넘음), **5(과제는 잘 풀리는데 표현은 나빠짐)**.
**틀림: 1(단조 감소할 것 = 가설 b)**, 3(kNN 거리 단조 감소 — ep1 이후 다시 는다).

## §3 을 지켰다

**`epoch=3` 을 채택 arm 으로 올리지 않는다.** `test_dev` 성능으로 고른 값이다.
어차피 fuse3 를 못 넘으므로 별도 사전등록으로 재확인하지도 않는다.

---

# 21차 사이클 — fuse4, 학습된 신호를 융합에 더한다 (2026-08-23 21:15~22:00)

## 무엇을 왜 했는가

17차에서 `O2(12 epoch) ⊕ fuse3` 를 쟀고 한 번 지고 두 번 무승부였다.
**그런데 그 O2 는 과학습된 것이었다**(정정 16). 20차가 `epoch=3` 을 찾았고
단독 성능이 0.4038 → **0.6375** 로 올랐다. **훨씬 나은 신호를 넣으면 달라질 수 있다.**

**사전등록 §2 에 정직하게 적었다** — `epoch=3` 은 **`test_dev` 성능으로 골랐고 그건 튜닝이다.**
규약상 개발은 `test_dev` 에서 하는 것이 맞고 **그래서 봉인이 있다.**
그러므로 **이기면 봉인을 한 번 열어 확인**하고, 통과 못 하면 **안 연다.**

## 결과 — 기각. 평균 이득이 +0.0001 이다

| arm | AUPR블록 | fuse3 대비 짝지은 CI | FPR@95TPR |
|---|---:|---|---:|
| **fuse3 (대조)** | **0.8304** | — | 0.1877 |
| fuse4 s0 | 0.8238 | **[-0.0082, -0.0050] 짐** | 0.1835 |
| fuse4 s1 | 0.8292 | [-0.0029, +0.0005] **무승부** | 0.1721 |
| fuse4 s2 | 0.8383 | [+0.0057, +0.0099] 이김 | 0.1645 |

**반증 조건 1 발동**(세 seed 전부 이겨야 한다).

**반증 조건 6 이 이 사이클의 문장이다** — **세 seed 폭 0.0145, 평균 이득 +0.0001.**
**seed 폭이 평균 이득의 100배가 넘는다.** 사전등록대로 **무승부로 적는다.**
seed 하나만 봤으면 "+0.008 이겼다" 도 "-0.007 졌다" 도 쓸 수 있었다.

**반증 조건 4 도 발동한다**: `562-776`(train-none 387장의 진짜 커버리지 구멍)이
**세 seed 다 나빠진다**(0.7785 / 0.7799 / 0.7876 대 0.7950).
반면 `1090-1334` 는 세 seed 다 좋아진다.

**`test_sealed` 는 열지 않았다.** 조건이 성립하지 않는 것이 기본값이다.

## 숨기지 않을 것 — 95% 재현율에서는 세 seed 다 낫다

| arm | 재현율 95% precision / 헛경보 |
|---|---|
| fuse3 | 0.264 / 15,370 |
| fuse4 s0 / s1 / s2 | 0.268 / **15,028**, 0.281 / **14,096**, 0.290 / **13,471** |

FPR@95TPR 도 세 seed 다 낮다. **총합 지표는 안 움직이는데 높은 재현율 끝에서는 일관되게 낫다.**
**그래도 채택하지 않는다** — 운영 지점 하나가 좋다고 판정 규칙을 고쳐 쓰면
그건 사후 지표 변경이고 **정정 9 가 정확히 그거였다.**

## 척도를 확인해서 큰 오보를 피했다

O2 사전등록 §3 은 **"재현율 80% 헛경보가 2,167장 아래면 천장 분석이 틀렸다는 증거"** 라 했다.
`fuse4 s2` 가 **2,051장**이라 얼핏 걸린다.

**걸리지 않는다. 2,167 은 공식 test 전수(118,595장) 척도이고 이 표는 `test_dev`(87,695장)다.**
dev 척도로 같은 계산을 하면 2,173 × 0.78 = **1,695장**이고 2,051 은 그 위다.

**그냥 비교했으면 "천장 분석이 틀렸다" 는 큰 주장을 잘못 낼 뻔했다.**
정정 12, 15 와 같은 실수 계열 — **잰 조건 밖으로 숫자를 옮기는 것.**

## 클래스별 — 학습된 신호는 Center 에 강하고 Edge-Ring 에 약하다

`O2 단독 s2` 의 Center AUROC 이 **0.9800** 으로 fuse3 의 0.9377 을 크게 넘는다.
반면 Edge-Ring 은 **0.8199** 로 크게 진다(fuse3 0.9533).
융합하면 Center 가 오르고 Edge-Ring 이 내린다. **CI 는 겹쳐 반증 조건 2 는 미발동.**

## 예측 채점 — 5개 중 2개 틀렸다

맞음: 2(기각될 것), 4(고유값 8만 이상), 5(부분 — `>1600` 이 s0, s1 에서 나빠짐).
**틀림: 1(넘을 것)**, **3(Scratch 가 가장 오를 것 — 실제로는 Center 다)**.

## 19차 보충 — 깊이 2 를 뒤늦게 쟀다

19차 §7 에 "2층을 안 잰다" 고 적어 둔 구멍을 판정 후에 메웠다.
**붕괴 비율이 세 seed 전부 깊이에 따라 단조로 준다** —
0.593/0.162/0.084, 0.576/0.195/0.112, 0.499/0.320/0.105.
**방향만이 아니라 기울기가 있다.**

**그런데 성능은 세 seed 다 깊이 2 가 최악이다**(0.0934 / 0.1592 / 0.1268).
**"붕괴 정도가 성능을 정한다" 가 다섯 번째로 부정됐다** —
붕괴는 깊이에 따라 매끄럽게 변하는데 성능은 그 축을 전혀 안 따라간다.

무작위 초기화의 유효차원도 깊이에 따라 는다(7.17 → 12.76 → 14.67).
**그래서 비율로 판정하기로 실행 전에 정한 것이 옳았다.**

---

# 22차 사이클 — seed 앙상블 (2026-08-23 22:15~23:00)

## 무엇을 왜 했는가

오늘 세 사이클(17, 20, 21)이 전부 같은 벽에 부딪혔다 — **초기화 잡음이 효과보다 크다.**
21차에서 세 seed 의 fuse4 가 0.8238 / 0.8292 / 0.8383 이었고 평균 이득이 +0.0001 이었다.

**하나를 고르면 test 로 고르는 것이고, 셋을 다 쓰면 잡음 자체가 줄어든다.**
seed 집합 `{0,1,2}` 는 **17차에서 결과 보기 전에 정한 것**이라 여기서 바꾸지 않았다.
결합 규칙도 **하나만** 정했다 — 여러 앙상블 방식을 재서 고르지 않는다.

**백분위를 평균한다. 점수를 평균하지 않는다.** seed 마다 kNN 거리 척도가 다르고
(17차 실측 0.0021~0.0884) 점수를 평균하면 척도 큰 seed 가 지배한다.

## 결과 — 무승부

| arm | AUPR블록 | fuse3 대비 | FPR@95TPR |
|---|---:|---|---:|
| fuse3 (대조) | **0.8304** | — | 0.1877 |
| **fuse5 앙상블 (주)** | **0.8312** | **[-0.0007, +0.0022] p=0.2467 무승부** | **0.1712** |

**이득 +0.0009 는 21차 seed 폭 0.0145 의 1/16 이다** → 반증 조건 2 발동.
`562-776` 이 여전히 나쁘다(0.7841 대 0.7950) → 반증 조건 5 발동. **봉인은 안 열었다.**

## 앙상블은 실제로 잡음을 줄였다 — 융합 뒤에 안 남을 뿐이다

**O2 단독은 앙상블로 확실히 좋아진다**: seed 별 0.6202 / 0.6474 / 0.6450 → **앙상블 0.6679.**
**세 seed 중 최고보다도 +0.0205 높다.** 백분위 평균이 초기화 잡음을 실제로 지운다.

**그런데 세 요소와 융합하면 그 이득이 사라진다**(+0.0009).
융합 점수를 A, B, C 가 이미 지배하고 **O2 는 네 번째 표 하나일 뿐**이다.

## 예측 3 이 맞았고 그게 이 사이클의 문장이다

실행 전에 **"`562-776` 은 여전히 나쁠 것 — 세 seed 가 다 나빴던 것은 초기화 잡음이 아니라
그 구간의 체계적 문제이기 때문"** 이라 적었고 맞았다.

> **앙상블은 초기화 잡음은 줄이지만 체계적 약점은 못 고친다.**

`562-776` 은 **train-none 이 387장뿐인 진짜 커버리지 구멍**이고,
18차가 `1090-1334` 에서 train-none 218장의 89%가 문턱을 넘는다고 실측한 그 계열이다.
**학습된 신호를 더하면 그 구멍이 더 아프다 — 학습도 같은 train-none 에서 했기 때문이다.**

## 클래스별 — 학습된 신호는 Center 전문가다

**O2 앙상블 단독의 Center AUROC 이 0.9784 로 채택 arm 전체(0.9377)를 크게 넘는다.**
반면 Edge-Ring 은 0.8122 로 크게 진다(0.9533).
"수용영역 7x7 이라 덩어리는 보고 테두리는 못 본다" 는 **설명이지 측정이 아니다. 안 쟀다.**

## 예측 채점 — 5개 중 1개 틀렸다

맞음: 2(잡음 안), **3(`562-776` 여전히 나쁨)**, 4(FPR@95 개선), 5(고유값 8만 이상).
**틀림: 1(이길 것)** — 무승부다.

## 세 사이클 연속 같은 축에서만 이긴다

21차와 22차 모두 **높은 재현율 끝에서만** 일관되게 낫다
(FPR@95TPR 0.1877 → 0.1712, 95% 재현율 헛경보 15,370 → 14,021).
**주 지표를 사후에 바꾸지 않는다**(정정 9). 대신
`candidate/ood_high_recall_arm.md` 로 **출처를 명시해 넘겼다** —
가설이 `test_dev` 에서 생겼으므로 같은 자료로 확인하면 순환이고,
**봉인은 한 번뿐이라 최종 arm 이 확정된 뒤에 써야 한다.**

## 마무리 — 문서와 결과가 조용히 갈리는 것을 막는 시험 (2026-08-23 23:20)

`docs/reports/OOD.md` 는 수십 개 수치를 인용하는데, 스크립트를 고쳐 다시 돌리면
**수치는 바뀌고 문서는 안 바뀐다.** 이 워크스트림은 이미 바닥 수치를 시험으로 박아 뒀고
(`test_a21_ood_baseline_pin.py`, `test_a24_local_density_pin.py`)
그 관례를 **오늘 만든 결과들**로 넓혔다 — `tests/test_result_pins.py`, 시험 12개.

박은 것은 대표 수치만이 아니라 **결론이 서 있는 성질**이다.

- 채택 arm 의 전수 수치(0.9698 / 0.8325 / 0.1719 / 고유값 3,953)와 클래스별
- `test_dev` 기준선 0.8304 / FPR@95TPR 0.1877
- **합 규칙 셋이 꼬리 강조에 단조**(0.7800 < 0.8187 < 0.8304) — 15차의 결론
- **Tippett 의 95% 문턱 동점이 8,776장** — 기각 사유가 꼬리가 아니라 눈금이라는 것
- **학습량 곡선이 세 seed 다 `epoch=3` 에서 봉우리** — 정정 16 의 근거
- **붕괴 비율이 세 seed 다 깊이에 따라 단조** — 19차의 기작 주장
- **fuse4 의 seed 폭이 평균 이득의 10배 넘음** — 21차의 판정 근거
- **`>1600` 헛경보 CI 가 `1334-1600` 과 안 겹침** — 18차

`result/` 는 대용량 저장소 심링크라 git 에 없다. **없으면 건너뛴다**(통과로 안 보이게 이유를 남긴다).

**파이프라인을 다시 돌리지 않는다** — 채택 arm 한 번 재는 데 5분이라 단위 시험에 못 넣는다.
**저장된 결과를 검사하므로, 결과를 다시 만들면 이 시험이 잡는다.**

---

# 23차 사이클 — 크기 비례 창 (2026-08-23 23:20~23:45)

## 무엇을 왜 했는가

18차와 22차가 같은 곳을 가리켰다 — **큰 웨이퍼가 조직적으로 더 걸리고**(8~84배),
**앙상블로 seed 잡음을 지워도 `562-776` 은 안 고쳐진다**(체계적 약점).

보고서는 채택 신호가 **"전부 크기 불변"** 이라 적었는데 **그 문장이 반쪽이다.**
요소 A, C 는 **다이 개수로 고정된 창**이라 5x5 창이 덮는 **웨이퍼 비율**이
다이 518개에서 4.8%, 2,393개에서 **1.0%** 다.
**다이 단위로는 불변인데 웨이퍼 비율로는 아니다.**

`k = 5·sqrt(die_size/518)` 로 창을 크기에 비례시켰다.
`die_size` 는 정상 웨이퍼에서도 아는 값이라 **범위 A** 다.
**S0(창만) 대조군을 반드시 뒀다** — 없으면 창의 기여와 보정의 기여를 못 가른다(14차의 교훈).

## 결과 — 크게 기각

| arm | AUPR블록 | fuse3 대비 | `>1600` 정상 헛경보율 |
|---|---:|---|---:|
| **fuse3 (현행)** | **0.8304** | — | 0.3991 |
| S0 창만 (통합 보정) | 0.7439 | [-0.0925, -0.0799] 짐 | **0.3206** |
| S1 창+층별 보정 (주) | 0.6772 | **[-0.1616, -0.1453] 짐** | **0.9484** |

**층별 보정이 도움이 되기는커녕 더 해롭다**(S1 vs S0 [-0.0726, -0.0608]).
반증 조건 1, 2, 3, 5 가 다 발동한다. **봉인은 안 열었다.**

**기작 가설은 부분적으로 지지된다** — S0 이 `>1600` 정상의 헛경보율을 **-20%** 내린다.
**그러나 큰 웨이퍼를 고치는 대가로 dev 의 다수인 중간 크기를 망친다.**

## 예측 4 가 크게 틀렸고 그게 이 사이클의 산출물이다 — 정정 17

"큰 웨이퍼만 창이 바뀌니 총합은 ±0.01 안" 이라 예측했다. **-0.153 이었다.**

| 요소 A 의 창 | train-none | test_dev |
|---:|---:|---:|
| k=5 | **22,917 (78.6%)** | 30,417 (34.7%) |
| k=7 | 2,250 (7.7%) | **39,366 (44.9%)** |
| k=9 | 2,613 (9.0%) | 17,436 (19.9%) |

**dev 의 다수(k=7, 39,366장)에 train-none 참조가 2,250장뿐이다.**
"큰 웨이퍼만" 이 아니라 **dev 의 3분의 2가 시끄러운 참조로 채점된다.**

뿌리를 재보니 **train-none 의 77.3% 가 `400-562` 인데 dev 정상은 13.8%** 다
(중앙값 **518 대 844**). **구멍 두 개가 아니라 참조 분포 자체가 평가 분포와 다르다.**
**숫자는 보고서 표에 이미 있었고 읽기를 잘못했다.** → **정정 17.**

## 층별 보정이 왜 더 나빠졌나 — 18차가 이미 답을 갖고 있었다

18차: **`1090-1334` 의 train-none 218장은 89%가 문턱을 넘는데 dev 정상은 16%다.**
**희소 층의 train-none 은 그 층의 dev 정상을 대표하지 못한다.**
층별로 보정하면 그 대표성 없음이 **증폭된다** —
`>1600` 정상의 헛경보율이 0.3991 → **0.9484** 로 거의 전부가 됐다.

**층별 보정은 참조가 대표성이 있을 때만 옳다. 여기서는 아니다.**

## 예측 채점 — 5개 중 2개 크게 틀렸다

맞음: 1(S0 이 나쁠 것), 3(`1090-1334` 에서 나쁠 것), 5(부분 — S0 만).
**틀림: 2(`>1600` 에서 이길 것), 4(총합이 ±0.01 안)**.

## 이것이 다음 수를 바꾼다

**진짜 병목은 보정 방법이 아니라 train-none 의 크기 분포다.**
정상 데이터를 늘리는 가장 가까운 원천은 **미라벨 638,507장**인데
**거기에 결함이 섞여 있고 비율을 모른다.** `train-none 만 쓴다` 는 전제를 다시 따져야 하고
9-class 쪽에서 미라벨 사전학습이 **해로웠다**는 실측도 있다.
**별도 사전등록 없이 손대지 않는다.**
